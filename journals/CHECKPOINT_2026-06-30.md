# Checkpoints — 2026-06-30

## 23:07 — IIC PCF8584 completion: model cleared, work narrows to delivering polled completion
- **Working on:** Diagnosing why a node-0x40 read never registers `iic_ocp0` completion during `iic_init`'s early verify; reconciling the controller model against the PCF8584 datasheet.
- **Done since last checkpoint:**
  - Confirmed from the PCF8584 datasheet that **PIN is the polled-completion handshake**: PIN→1 on START, PIN→0 (active, "needs service") after a byte completes, PIN→1 again when S0 is read. Matches `iic_poll_rt` (`if (status.bits.pin == 0) iic_service(pb)`) and the EmulatR model (PIN=0 on ACK, P2 dummy, P3 NACK→0x08).
  - Traced a 1-byte node-0x40 read through `iic_service` against the model: completes in **three calls**, returns `rec_count == 1` → would register `iic_ocp0`. **Controller model is not the defect** — it is ready to be polled.
  - Updated `IicPcf8584.h`: replaced the false "interrupt completion fix" comment block with the proven mechanism — PC264 is `#define POLLED`, completion comes from the `krn$_create`'d `srom_poll` process, interrupt path is compiled out, and `interruptPending()` / `EMULATR_IIC_IRQ_BIT` are retained **inert** for the WEBBRICK path only. Doc-only, zero behavior change.
  - Marked `DS20_Badge_IIC_Completion_Interrupt_topic.xml` **[SUPERSEDED]**; created new `DS20_Badge_IIC_Polled_Completion_topic.xml` (well-formed XML verified). Memory updated with the refined finding.
- **Open / next:** Deliver the **polled** completion — get `srom_poll` dispatched to service node 0x40 during `iic_init`'s early verify. Decisive A/B measurement still pending: **(A)** cooperative scheduler not running the just-created process (broad case, would also explain the FDC and IDE/ATAPI stalls) vs **(B)** a node-0x40 PIN-presentation detail. Evidence (~3 controller touches) leans hard toward **(A)**. Immediate next: prep the A/B run script, or check whether `srom_poll`'s `krn$_create` is reached in EmulatR before the `iic_init` verify.
- **Watch-outs:** D: mount bash view showed a **phantom truncation** — superseded XML read as 40 lines via bash/`wc`, but host-side Read shows 49 lines, balanced tags. Do not trust `wc`/parse over this mount; verify host-side with Read.

## 18:07 — A/B diag arm fired; now analyzing 1.04 GB retire trace for iic_ocp0 registration
- **Working on:** Session "EmulatR DS20 badging testing" — reading `traces/20260630-175633_srm.trc` (1.04 GB) in bounded slices to answer the crux: did `iic_init` register `iic_ocp0` after the successful node-0x40 read, or not?
- **Done since last checkpoint:**
  - Built/repaired `tools/diag_ds20_badge_ab.sh`: launch line corrected to `./Emulatr.exe` (line 136), build resolver now targets `out/build/relwithdebinfo` first, summary uses a `cnt()` helper (fixed prior `0\n0` garbage output).
  - Clean A/B run completed against the RelWithDebInfo build (17:40): **arm fired** — `IIC-TRACE-ARM node=0x40 -> retire window 4000000 instrs`. Counts: IIC-TXN 229 total / 171 node-0x40; IIC-RD 8/2; IIC-CTRL 40/2. Retire trace captured to `traces/20260630-175633_srm.trc`.
  - Run stopped on `MaxCyclesExceeded` at PC=0x8321, cycles=250000000 (250M cap is a safety bound, not a target). Clarified: badge/SYSVAR markers empty because `build_hwrpb`/`get_sysvar` is downstream of a **pre-banner console wait** ("ISA table corrupt! / Initializing table to defaults") and never runs unattended — so truncation isn't losing the badge; the badge simply isn't computed yet at 250M. Node-0x40 verify event already occurred at ~184.64M ord and is inside the captured window.
  - Located IIC controller in the trace at physical `0x800fff8000` (offset 0 = S0 data, 1 = S1 status/control); began extracting controller accesses near the node-0x40 START.
- **Open / next:** Slice the .trc around ord ≈184.64M to determine whether `iic_init` registers `iic_ocp0` after the read. If **not registered despite reading the byte** → that's the defect (in this window). If **registered** → miss is later in `get_sysvar`'s `fopen`, reachable only by driving the console (LFU → update srm → reinit), which runs well past 250M.
- **Watch-outs:** Whole-file scans on the 1.04 GB trace will time out — bounded slices only. D: mount still serving **inconsistent bytes** to the sandbox (same file read as 651 NUL bytes then 0 seconds apart); on-disk file verification requires host-side Git Bash. Earlier `tr: unknown option -- firmware` failure traced to edit corruption over the flaky mount, not user error.

## 20:08 — DS20 badge landed; tracing the 100 MHz banner to correct CPU speed
- **Working on:** Session "EmulatR DS20 badging testing" (running). Badge task is **done and verified** ("AlphaServer DS20"); focus has shifted to the CPU-speed banner reading **100 MHz** instead of the DS20's true 500 MHz. Session paused at an AskUserQuestion on how to fix.
- **Done since last checkpoint:**
  - DS20 badge **fixed and verified**; topic/journal/memory updated; test plan written (permutation matrix, regression guards, ES40 identity framework, with `boot`/`-fl` deferred to the guest-OS scaffold).
  - Root-caused the 100 MHz banner via source trace, **not a measurement and not from EmulatR's clock**: banner string is `pc264.c:445 "AlphaServer DS20 %3d MHz"`, the `%3d` fed by `cns$srom_cycle_cnt`. Banner math at `kernel.c:2241` = `krn$_timer_get_cycle_time(whoami()) / 1000000`, returning `100e6`.
  - Mechanism: EmulatR identifies as **`ISP_MODEL`**, so firmware **skips** `timer_check()` and `krn$_timer_set_cycle_time()` (`kernel.c:1853-1860`, the "TURBO HACK - set_cycle_time() not implemented in ISP" at kernel.c:1856), leaving cycle time at the static `HWRPB$_CC_FREQ` field (`timer.c:1458` path) → ~100 MHz. Decoupled from EmulatR's actual cycle model.
  - Surfaced the fix tradeoff: EmulatR's real modeled clock `kProfileAlphaClockHz` = **2^28 = 268 MHz** (a 2026-06-02 boot-speed hack, lowered from 1e9 to make interval-timer ticks ~4x faster; drives interval timer `profileAlphaClockHz/1024` and RTC). A separate `cycle_count_freq_hz = 500'000'000` exists in FirmwareDeviceManager but the banner never reads it. Tying the banner to `kProfileAlphaClockHz` would report 268 MHz, not the true DS20 500 MHz.
- **Open / next:** Decide the CPU-speed approach (AskUserQuestion pending): drive banner from a static 500 MHz field vs. from `kProfileAlphaClockHz` (currently 268 MHz). After reaching `P00>>>`, run configuration tests and **verify NVRAM persistence** — save NVRAM, reload, confirm changed settings display (Tim's requested next step).
- **Watch-outs:** `kProfileAlphaClockHz` is an experimental boot-speed value (268 MHz), so a "reflect actual cycles" fix would report a non-authentic speed — resolve the static-vs-dynamic decision before editing. D: mount byte-inconsistency persists; verify all writes host-side with Read.
