# Checkpoints — 2026-06-29

## 16:50 — Git-syncing last week's offline EmulatR work onto this system
- **Working on:** Active session ("Git pull offline sync") is figuring out how to bring last week's offline work down to this machine. Open question is whether `git pull` is the right command — answered: only if the work was pushed to a remote; if it lives on another machine (the prior handoff targeted a Mac), it must be pushed first. Recommended diagnostics: `git remote -v`, `git fetch`, `git log --oneline --all --since="2 weeks ago"`, `git status`.
- **Done since last checkpoint:** Prior "EmulatR project migration" session completed the SSOT rehome — `memory.md` and `CLAUDE.md` moved to repo root (`…/EmulatRAppUniV4/Emulatr/`), plus the resume journal `journals/HWRPB_Region_Fidelity_and_Resume_20260624.md`. Verified the `ds20_v7_3_platform.json` / `ds25_v7_3_platform.json` "modified with net deletions" diff was a **FUSE mount truncation artifact**, not a real regression — OCP pair (0x40/0x42) intact at lines 13–15; native git shows clean.
- **Open / next:** Run the git diagnostics above on this system to confirm whether last week's commits are reachable via the remote, then `git pull` (or push-from-source-machine first). Pending decision: whether to append the proposed CMake/MSVC + runtime/scratch `.gitignore` stanza before the next commit to silence the "few dozen uncommitted files" VS2022 is showing.
- **Watch-outs:** Do NOT trust the sandbox/FUSE view for git or file integrity — do all git operations natively (Windows Git Bash), as truncation phantoms have hit `.gitignore`, `ds20`, and `ds25`. Keep `firmware/` ignored (copyrighted DEC/Compaq SRM images, multi-MB — a 177 MB blob previously got a push rejected). Don't commit build output (`*.dir/`, `out/build/`, `CMakeCache.txt`, `*.obj/*.exe/*.pdb`) or runtime scratch (`*.img`, `*.axpsnap`, logs, `ds*_flash.rom`, `EMULATR_STOP`). Delete the stray duplicate `tools/ghidra_scripts/dumSysVarFns2.java`.

## 17:07 — Git sync diagnosis resolved: Mac work never pushed (slow connection), now reconciling
- **Working on:** Same active session ("Git pull offline sync"). Diagnosis is now nailed down. Remote `origin` = `https://github.com/EmulatRApp/EmulatRAppUni.git`. Hash comparison across the three copies: GitHub `origin/main` stuck at **`6203a4a`** (June 24); this PC's `main` at **`56320af`** (June 24, "Miscellany Checkin for mac environment pull synchronization") — 1 commit ahead; the Mac at **`1754755`** (June 28 18:27, "LL/SC reservation Mode, ES40…") — several days further. Conclusion: the Mac's daily commits were committed but **never pushed** (the slow connection silently broke the pushes), so there is nothing on GitHub to pull — direction is reversed (Mac must push up first, then PC pulls).
- **Done since last checkpoint:** Walked Tim through the diagnostics (`git fetch`, `git status`, `git log -3 --format`, `git ls-remote origin refs/heads/main`) that proved GitHub hasn't advanced since June 24. Established both PC and Mac hold commits GitHub lacks → histories may have diverged; combined with the June 23 blob-strip rewrite (branch `backup-pre-blob-strip-20260623`), warned hard against any `git push -f`. Advised Tim NOT to re-commit on the Mac (commits are local-only; the problem is the push). Tim reported it "looks better" (push from Mac appears to have gone through).
- **Open / next:** Active session just requested folder access (`request_cowork_directory`) to read `memory.md` and the journals created since 6-24, per Tim's last request. Next: confirm Mac→GitHub push landed (`git ls-remote` should now show `1754755`), then `git pull` on this PC and reconcile the PC's `56320af` with the Mac line (prefer `--rebase`, never force).
- **Watch-outs:** Never `git push -f` while the June 23 blob-strip rewrite is in play — it could erase the PC's `56320af` commit. Slow-connection pushes can silently fail; let any push run to completion and verify with `git ls-remote origin refs/heads/main` afterward. Working-tree still shows uncommitted `journals/CHECKPOINT_2026-06-24.md` (modified) and untracked `journals/CHECKPOINT_2026-06-29.md` — handle before/after the merge so they aren't lost.

## 19:06 — Session pivoted to a 14-task work board across three pillars (HWRPB / badge / SMP)
- **Working on:** Same active session, now post-sync, laid out 14 tasks (TaskCreate ×6 + TaskUpdate dependency chains) for the three pillars Tim named: HWRPB region mapping, "AlphaPC 264DP" badge recognition, and chipset/SMP. Currently blocked on one clarification from Tim (task 13, see below).
- **Done since last checkpoint:** Mapped the HWRPB firmware→OS hand-off contract at top level. SRM builds a single HWRPB at **PA 0x2000**, directory chains to TBB **0x2140**, two per-CPU slots **0x2180 / 0x2400** (stride **0x280**), CTB **0x2680**, CRB **0x27e0**, MEMDSC **0x2840**, DSRDB **0x2ac0**, GCT/FRU tree at **0x3ff32000**. Reframed the "AlphaPC 264DP" badge as the *first detected divergence* in that contract — a firmware SysType decision (member 1 = default), NOT an IIC/OCP bus gap (bus layer proven correct on the wire). SMP harness built and green (Sequential == Threaded, bit-identical) but only one real CPU wired.
- **Open / next:** Tasks 1–5 HWRPB: field-by-field scan (MEMDSC first); reconcile live slot stride **0x280 vs Hwrpb.h static_assert 0x400**; resolve two-HWRPB question (PA-0 hardcoded builder vs SRM's PA-0x2000 copy — which the OS actually consumes); stand up Gate B (region as inherited at `boot` diffed vs `>>>` snapshot); lock behind a boot-time validator. Tasks 6–7 badge: aim `EMULATR_PA_WATCH` at SYSVAR base **+0x58** (resume doc's +0x88 was a hex/dec slip) to capture writing PC, then Ghidra `get_sysvar`/`build_dsrdb` to learn why member 1; fix family-wide (DS20 → DS10/ES40 inherit). Tasks 8–12 SMP chained: P0 cleanups → re-entrant `AlphaCpuAgent` (byte-identical gate) → N-CPU scheduler + per-CPU interrupt-divert indexing → LL/SC cross-CPU interlock (the hard part) → IPI + gated secondary bring-up.
- **Watch-outs:** **Awaiting Tim's answer on task 13** — he flagged "**kKeyValue addressing**" as a reveal to document, but that token isn't in any journal/source (only unrelated `kKeyValue` in `traceLib/BreakpointSink`/`DecListingSink`). Working hypothesis: he means HWRPB self-relative offset-table addressing (pointers relative to base 0x2000, per-CPU slot by stride 0x280, SYSVAR member = `(SYSVAR>>10)&0x3F`) — needs confirmation before documenting. Stride conflict 0x280 (live) vs 0x400 (static_assert) is unresolved. SYSVAR offset is **+0x58**, not +0x88.

## 21:07 — Silicon-path comparison launched; ISP-path source trace hits a wall
- **Working on:** Same active session. Investigating whether the "264DP" badge is an artifact of the **ISP (virtual) execution path** reordering/skipping hardware init vs the **silicon (REAL_HW) path**. Tim has kicked off a silicon-path run (long init) and asked to document the two methods while it runs.
- **Done since last checkpoint:** (1) Source-traced `iic_init()` across the whole `palcode` tree — it has **no caller** in this `ref` snapshot (decl iic_driver.c:152, def :963, `iic_inited` global, plus a 1991 filesys.c comment added 11-Mar / removed 12-Mar-1991 "so it'll work on h/w"). `filesys_init()` (529–578) does NOT call it. Yet the trace proves `iic_init` *does* run (the 0x40/0x42/0x4e/0xa2 node-probe loop = its inode-creation loop, iic_driver.c:1061) → the shipped **V7.3-2 binary** is newer than this read-only apisrm SRM source, so the `iic_init`-vs-`build_hwrpb` order **cannot be pinned from source** — binary is the only ground truth. (2) Identified the ISP/silicon lever: **`EMULATR_PLATFORM`** in `MemDrainer.h` (the 0xBFFC read-intercept). Default ISP intercepts 0xBFFC→`0xCAFEBEEF`; `EMULATR_PLATFORM=silicon` disables the intercept so `platform()` reads real 0xBFFC → REAL_HW path. (3) Gave Tim the silicon run command (silicon + `EMULATR_IIC_TRACE=1` + `EMULATR_PA_WATCH=0x2058`, `ds20 cold`, tee to `fw_ds20_silicon.out`) plus compare greps. (4) Wrote/validated H&M XML doc `Virtualization_Methods_topic.xml` (well-formed, table preserved) capturing the two paths.
- **Open / next:** Wait for the silicon run (may not reach `>>>`). Diff vs ISP run on three questions: does the **0x40 IIC probe** happen *earlier* (before `build_hwrpb`) on silicon → would let `fopen("iic_ocp0")` succeed and badge DS20; does **`PA-WATCH` fire** on silicon when it didn't on ISP; where does it diverge/stall vs the ISP run's ~182M-cycle decompression + IIC scan. Also still pending: the cheap `EMULATR_PA_WATCH=0x2000` probe (watch `build_hwrpb`'s first store, the self-pointer `hwrpb->BASE = virt_to_phys(hwrpb)` at hwrpb.c:371) to settle the two-HWRPB question.
- **Watch-outs:** Code-comment caveat at the lever (`MemDrainer.h:463-464`): **REAL_HW is "currently unmodeled → stalls"** — the silicon path will likely grind in unmodeled real-HW timing/probe/IDE steps and run to `MaxCyclesExceeded`; stop cleanly by touching `EMULATR_STOP` in the run dir. The value is the *comparison*, not reaching the prompt. On ISP, `EMULATR_PA_WATCH=0x2058` **never fired** (either MemDrainer isn't observing those guest stores = a watch-coverage gap, or 0x2000 HWRPB isn't guest-written at all = host-built HWRPB).

## 23:07 — GuestMemory DRAM-commit instrumentation landed to close the PA-WATCH coverage gap
- **Working on:** Same active session pivoted from the silicon/ISP compare to fixing the root cause of the prior watch-out — that `EMULATR_PA_WATCH=0x2058` never fired on ISP. Added two observe-only instruments at the **universal DRAM commit sink** in `GuestMemory.cpp` so guest stores MemDrainer misses are still caught at the point of commit.
- **Done since last checkpoint:** Implemented and wired **7 guard blocks** in `GuestMemory.cpp` (1 include + 1 helper + 5 call sites), all under `#if defined(EMULATR_DIAGNOSTIC_LOGGING)` (compiled in Debug/RelWithDebInfo, stripped in Release). Two new levers: **`EMULATR_GMEM_WATCH=<pa>`** — a store-watch at the commit sink that catches what `MemDrainer` missed (emits `GMEM-WATCH`); **`EMULATR_TRACE_ARM_PA[/VAL/INSTRS]`** — arms the existing retire `.trc` window on a store to a PA via `DecListingSink::setTraceWindowCountdown` (emits `GMEM-TRACE-ARM`). Authored + validated H&M topic `Diagnostics_GuestMemory_Trace_topic.xml` (XML well-formed) and design journal `20260629_guestmemory_diag_instrumentation.md` (exact diff + faithfulness statement + build/verify notes). Tracked as **task #16**.
- **Open / next:** On Tim's side: build a **RelWithDebInfo** config (NOT Release, or the `EMULATR_DIAGNOSTIC_LOGGING` guard strips the instrument), then run the recipe. If `GMEM-WATCH` / `GMEM-TRACE-ARM` lines appear, the instrument is live and the `.trc` should capture the `get_sysvar`→`fopen("iic_ocp0")` store stream that drives the badge decision.
- **Watch-outs:** Could **not compile in the sandbox** — unverified against a real build. If the `traceLib` include trips a link cycle, the design journal documents the one-line fallback. Faithfulness preserved (observe-only; no guest-state mutation), but confirm the guard truly gates: a Release build must show zero `GMEM-*` output.

## 01:07 — Two more observe-only trace levers (IIC arm + PA disarm) landed to bracket the registration window
- **Working on:** Same active session ("Git pull offline sync"). Added the START/END bounds for the retire `.trc` window so it can capture the `iic_init` device-registration sequence without flooding the trace. Implementation complete and self-consistent; awaiting a RelWithDebInfo rebuild + run on Tim's side.
- **Done since last checkpoint:** (1) **`memoryLib/GuestMemory.cpp`** — added `EMULATR_TRACE_DISARM_PA` (+ optional `EMULATR_TRACE_DISARM_VAL`) to `gmemDiagOnStore`: a store to that PA calls `setTraceWindowCountdown(0)` and prints `GMEM-TRACE-DISARM …` (the END bound; works because `0x2000` is DRAM the sink sees). (2) **`deviceLib/Tsunami/IicPcf8584.h`** — added `EMULATR_TRACE_ARM_ON_IIC` to `startTransaction`: fires once on the first START transaction (`0`/`1` = any node, else matches `addr & 0xFE`, e.g. `0x40`), arms the window for `EMULATR_TRACE_ARM_INSTRS`, prints `IIC-TRACE-ARM …` (the START — placed in the IIC model because the controller is MMIO and never reaches the DRAM sink). Verified **no include cycle**: both trace headers (`TraceSink.h`→`CommitRecord.h`, `DecListingSink.h`) are leaf, so the `traceLib/DecListingSink.h` include in deviceLib is safe. Docs updated: journal addendum (rationale + recipe) and the H&M topic table gained `-DEMULATR_TRACE_HOOKS=ON`, `EMULATR_TRACE_ARM_ON_IIC`, `EMULATR_TRACE_DISARM_PA` rows.
- **Open / next:** Tim does a **RelWithDebInfo** rebuild (both files changed) then runs the recipe — `EMULATR_TRACE_ARM_ON_IIC=0x40` (arms at `iic_init`'s first 0x40 probe, ~cyc 185M), `EMULATR_TRACE_ARM_INSTRS=4000000` (~4M covers registration), `EMULATR_TRACE_DISARM_PA=0x2000`/`VAL=0x2000` (backstop at `build_hwrpb` entry, ~37M cyc later), `EMULATR_GMEM_WATCH=0x2058`, `EMULATR_IIC_TRACE=1`, fresh `ds20_flash.rom`, `unset EMULATR_PLATFORM`, `./run_fw.sh ds20 cold`. On "done", pull the `.trc` and read `iic_rw_common`'s probe of node `0x40` to find the `rec_count`/status that gates registration — the fix site for issue **#7**.
- **Watch-outs:** Old `EMULATR_TRACE_ARM_PA` (0x2000 arm) intentionally left **out** of the recipe so the IIC arm wins; both arm paths coexist (first to fire sets the countdown) if ever combined. With `ARM_INSTRS=4M` the countdown will almost certainly close the window before the `0x2000` disarm fires (~37M cyc later), so the disarm is just insurance — if 4M cuts off before `build_hwrpb`, bump it. Still **uncompiled in the sandbox** — cycle check was static (grep), not a real build.
