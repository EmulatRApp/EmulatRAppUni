# Checkpoints — 2026-06-14

## 19:06 — IRQ4 halt gate: staged to run the armed-trace disambiguation (World A vs C)

- **Working on:** The `b dqa1` "BOOT NOT POSSIBLE" halt gate. Goal this session is to run the staged diagnostic and capture the actual load that `halt_switch_in()` tests, to pick World A (clear a real source) vs World C (model a source read) *before* writing any `clr_irq*` code. (Today's working session `local_30e23842` has D:\EmulatR connected and is awaiting instructions; no new emulator changes landed yet today.)
- **Done since last checkpoint:** From the 2026-06-13 EOD session (`local_37375327`): smir (TIG `0x40`) root-caused and faithfully modeled (reads 0). `EMULATR_TIG_TRACE` canary pinned 4 unmodeled writes to `clr_irq4` = TIG+0x440 (pa `0x80130000440`, log lines 142025/142026/147065/147066). **World B ruled out from code:** Cchip `m_drir` resets to 0, RO from CPU, written only by device assert/deassert, returns stored value — cannot read all-ones (`TsunamiCchip.h:356`, read at line 790). Staged runbook `tools/diag_halt_irq4.sh` (sets `EMULATR_TRACE_WINDOW=1`, `EMULATR_TIG_TRACE=1`, `EMULATR_TRACE_DIR=/d/EmulatR/traces`); added to `CMakeLists.txt` for persistence.
- **Open / next:** Run from build dir `out/build/relwithdebinfo`: `./tools/diag_halt_irq4.sh`. At `>>>` examine `e pmem:801a0000300` (DRIR, expect 0/`<55>`/`<63>`, NOT all-ones), `e pmem:801a0000280` (DIR0), `e pmem:80130000040` (smir, expect 0), then **READ** `e pmem:80130000FF8` to ARM (must return `7FFFFFFFFFFFFFFF`), then `b dqa1`. Stop cleanly from a 2nd shell: `touch <build>/EMULATR_STOP`. The `_srm.trc` load (`a=<pa> v=<val>`) just before "BOOT NOT POSSIBLE" is the gate → settles World A vs C.
- **Watch-outs:** Arming is a **READ** of `pmem:80130000FF8`; a *deposit* disarms. Trace sink only exists if `EMULATR_TRACE_WINDOW=1` (the script sets it) — otherwise arm no-ops and `_srm.trc` stays header-only (7 lines). Flush happens only on clean exit (`~Machine → ~DecListingSink`), so a mid-run empty trace is expected, not failure. Do NOT build the `clr_irq*` family until the trace proves the halt source exists — `clr_irq4` is currently a clue/symptom, not a proven gate (apisrm "IRQ4" refs are ISA COM1 serial, not front-panel halt).

## 21:07 — FP backend wiring: plan locked, awaiting (A) vs (B) before Slice A edits

- **Working on:** Pivot away from the IRQ4 halt gate to FP execution. Session `local_30e23842` (D:\EmulatR connected) is wiring the FP grain-leaves to a live backend per the FP audit journal the user supplied. Plan presented; blocked on a one-bit user decision before editing `GrainMasterV4.tsv` / `handwritten.tsv` and the leaves.
- **Done since last checkpoint:** Read the codegen contract + leaf/backend plumbing. Confirmed seams: leaves are `exec*(InstructionGrain const&, ExecCtx const&) -> BoxResult`; `c.cpu->fpcr` is reachable, `c.opA/opB` carry operand bits, `FPVariant` carries decoded rounding/trap qualifier. Found the gap: the only `IFpBackend` instance today is the x87 guard self-test in `main.cpp` — no leaf reaches a backend. SoftFloat backend implements every IEEE kernel but nothing calls it; native SSE2 backend (handoff Phase 4) was never built.
- **Open / next:** Awaiting user pick — **(A)** ship `fpBox::activeBackend()` returning SoftFloat now, wire all leaves, add native SSE2 as drop-in later behind `EMULATR_FP_SOFTFLOAT` (**recommended**); or **(B)** build native SSE2 first. Leaf edits are identical either way (all call `activeBackend()`). On the nod, start **Slice A** (no codegen): rewire existing `execAddt/Subt/Mult/Divt`, `execCmpteq/tlt/tle` to `activeBackend()`. New seam to add: `fpBoxLib/fp_backend_active.h/.cpp`. Then Slice B (codegen: ADDS/SUBS/MULS/DIVS promote + CVT* / SQRT* rows), C (VAX G/F — the OpenVMS gate), D (FP branches/FCMOV/CMPTUN/FTOIS/ITOFx), E (FPCR fold + DYN rounding + trap delivery).
- **Watch-outs:** Fold is **result-first** — wire arithmetic bits now, defer `FpExc → FPCR` sticky-flag fold to Slice E; FPCR layout is `_PROVISIONAL` until `fpcr_layout_hrm_verification.md` is confirmed, so do not bind a guessed layout. `genGrains.py` runs in the sandbox (show diff); the MSVC build + doctest run stays on the user's machine. Each codegen slice must end in a regen + diff review so hand edits survive regeneration.

## 23:07 — FP Slices A+B landed; final regen blocked by a sandbox mount glitch (run on host)

- **Working on:** Session `local_30e23842` (D:\EmulatR connected) executed the FP backend wiring plan — picked option (A): `fpBox::activeBackend()` returns SoftFloat now, all leaves call it, native SSE2 deferred behind `EMULATR_FP_SOFTFLOAT`.
- **Done since last checkpoint:** **Slice A** rewired `ADDT/SUBT/MULT/DIVT` + `CMPTEQ/CMPTLT/CMPTLE` from the POC to `activeBackend()`. **Slice B** promoted `ADDS/SUBS/MULS/DIVS` from stubs and added new ops: `CMPTUN` (16.0A4), `CVTTS` (16.0AC), `CVTST` (16.2AC), `CVTTQ` (16.0AF), `CVTQS` (16.0BC), `CVTQT` (16.0BE), `SQRTS` (14.08B), `SQRTT` (14.0AB) — func codes from `Processor Support/alpha_arch_ref.txt`. New seam `fpBoxLib/fp_backend_active.{h,cpp}` (+`FPBOX_SOURCES`); `fBoxLib/grains/FpExec.h` gained `fpVariantFromEncoded()` (raw-field qualifier decode) + `foldFpcrExc()` (sticky fold via `ArithmeticStatus`). TSV: 8 base rows in `GrainMasterV4.tsv`, 8 leaves in `handwritten.tsv`. `genGrains.py` `ItFp` sub-table grown 128 to 2048 (SQRT func codes exceed 128). Host Grep confirmed all new rows (CMPTUN, CVTST, CVTTQ, SQRTS, SQRTT) well-formed and `REMQHILR` line 665 intact.
- **Open / next:** **On the host machine, run `grainFactoryLib/codegen/invocation.bat`** to do the final full regen, review the generated diff, then build + run FP doctests. The single-op regen completed cleanly mid-session, so binding should be clean. Then user must choose: extend `expandFpVariants.py` for conversion/SQRT rounding+trap variants, OR move to Slice C (VAX G/F float, opcode 0x15 — the OpenVMS gate, needs `IFpBackend` extended with `addG/addF/...`).
- **Watch-outs:** **Final regen was NOT run in-session** — a sandbox mount sync glitch truncated the sandbox copy of `genGrains.py` (1469 lines vs full host file) and mangled an unrelated `REMQHILR` row, producing a spurious `REMQHILR references undefined flag S` error. This is mount-side corruption, NOT host source damage (host Grep verified clean). Still deliberately open: rounding/trap variants of CVT*/SQRT (base rows only added, matching compare-row convention); `FCMOVxx`, `CVTLQ/CVTQL`, `FTOIS`, `ITOFS/ITOFT` are **eBox** not fBox (already stubbed). FPCR sticky fold remains result-first per Slice E plan.

## 01:07 — FP per-func distinct leaves: FloatVariants.cpp (216 leaves) landed; awaiting host regen + build

- **Working on:** Session `local_30e23842` (D:\EmulatR connected) expanded the FP wiring from the 8-op Slice A+B set to a **full per-`opcode.func` distinct-leaf** scheme — one leaf per FP function code instead of shared variant dispatch.
- **Done since last checkpoint:** New `fBoxLib/grains/FloatVariants.cpp` — **216 distinct leaf functions, one per FP `opcode.func`**: **79 real** (every IEEE conversion/sqrt/compare variant wired to the SoftFloat backend with runtime qualifier decode + FPCR fold) and **137 explicit `// TODO(fp-leaf)` stubs** (VAX F/G [106], FCMOV, CVTLQ/CVTQL, FP branches FBxx — return 0). New generator `fBoxLib/grains/codegen/gen_fp_leaves.py` produces all 216 (clean `/tmp` run = 79 real + 137 stubs). All 216 registered in `handwritten.tsv` so codegen routes to the real leaves (no more `logUnimplementedStub`). `FloatVariants.cpp` added to `CMakeLists.txt` (app + test targets). Host Grep verified the file intact: namespace opens line 16, closes line 2467, 216 distinct leaf functions present. Repaired a mount-write corruption: a bash append clobbered 13 lines of the FBOX block in `handwritten.tsv` (truncated `fBo` at line ~281, clobbered `execCpys`…`execMtFpcr`) — restored via host Edit.
- **Open / next:** **On host:** run `grainFactoryLib/codegen/invocation.bat` (genGrains.py) to regenerate dispatch so the 216 bind to `FloatVariants.cpp` leaves → MSVC build (79 IEEE leaves compute real results, 137 stubs return 0) → run FP doctests to confirm no regression vs the 424/424 baseline.
- **Watch-outs:** **Sandbox bash mount is unreliable for WRITES this session** — the bash append already corrupted `handwritten.tsv` once; keep all file edits on the host Edit/Write tools, never bash redirection. VAX (106 stubs) need `IFpBackend` extended with `addF/addG/…cmpG/cvtG*` kernels, then a regen upgrades them automatically. FCMOV/CVTLQ/CVTQL + FBxx need custom bit/branch bodies; ITOFS/ITOFT/ITOFF are **eBox** (intentionally not in `handwritten.tsv`) — small eBoxLib follow-up. Generator is re-runnable: regenerating upgrades stubs to real leaves as backend kernels land.

## 03:07 — Instruction coverage closed to a single known stub (SWPCTX); ITOF + REMQ*R + FBxx landed

- **Working on:** Session `local_30e23842` (D:\EmulatR connected) finished the 7 mechanical instructions remaining to drive instruction coverage to completion, so any boot anomaly that isn't `SWPCTX` cannot be a missing instruction.
- **Done since last checkpoint:** **ITOFS/ITOFT/ITOFF** given real bodies in `eBoxLib/grains/IntArith.cpp` (reverse of FTOIT: `ITOFT` = raw 64-bit copy, `ITOFS`/`ITOFF` via `convertS_/convertF_FloatingToRegister`), registered in `handwritten.tsv`. **REMQHILR/REMQTILR/REMQHIQR/REMQTIQR** added as delegating leaves in `palBoxLib/grains/PalEntries.cpp` — the discovery: the "implemented" interlocked-remove siblings all just `return execCallPalDispatch(g,c)` (firmware PAL runs the queue algorithm), so these 4 were stubbed instead of delegated; now fixed and registered in `handwritten.tsv`. **FBxx** FP branches already wired into the generator earlier this session. Net result: after a regen+build, `GrainStubs.cpp` holds exactly one `logUnimplementedStub` — `SWPCTX` — which is intentional (only hit at first VMS process switch, downstream of the current blocker).
- **Open / next:** **On host:** run `grainFactoryLib/codegen/invocation.bat` (regenerates `FloatVariants.cpp` with real FBxx, drops ITOF/REMQ*R from the stub set since they are now in the manifest) → MSVC build. Then the real boot blocker is **Task #3**: `HW_MTPR 0x2D` reserved-IPR write → DTB-double-miss spin — everything else is downstream. Secondary: **Task #8** validate VAX F/G kernels against the SoftFloat differential harness (DIV is the trusted SIMH port; ADD/SUB/MUL/SQRT want cross-check) + port conversions onto `vax_float.h`; **Task #1** the `smir` flash-write-enable echo.
- **Watch-outs:** `SWPCTX` (Task #9) stays the single deliberate stub — implement when the trace reaches scheduling. The new `vax_float.h` kernels were authored here but could NOT be compiled or differential-tested in the sandbox — verify on host. Remember: run `invocation.bat` before building so generated leaves + dispatch are current; keep file edits on host tools (mount writes unreliable this session).

## 05:07 — Pivot to docs: V4 architecture Overview page rewritten from verified sources

- **Working on:** Session `local_30e23842` (D:\EmulatR connected) shifted from emulator code to the handbook/docs effort — rewriting the architecture **Overview** page against the actual V4 source tree, replacing a Claude-web draft that was substantially wrong for V4. No emulator code changed this session.
- **Done since last checkpoint:** Overview page rebuilt from verified V4 classes/files and re-presented. Corrections locked: **Memory** = `GuestMemory` is the sole interface (SafeMemory removed); sparse physical space paged on demand in 64 KiB blocks, `VirtualAlloc` (Windows) / `mmap` (Linux/ARM); MMIO routing + range faults belong to the `TsunamiChipset` arbiter, not memory. **Translation** = the **SPAM** TLB (Set-Partitioned Associative Memory) via `SPAMShardManager` ITB/DTB in `CpuState`, through `Ev6Translator`, epoch-based invalidation — no `Ev6SiliconTLB`. **Faults** = a `faultCode` on `BoxResult` delivered at WB via `Ev6EntryVectors` — no `FaultDispatcher`. **Pipeline** = `PipelineDriver`, six in-order stages **IF→DE→GR→EX→MEM→WB**. **Southbridge** = Cypress **CY82C693** (`Cypress_CY82C693ISABridge`), standard PCI bus/slot/function — not AliM1543C, no "stratum" model. Authoritative sources = Alpha ARM + DEC SRM/PALcode; AXPBox is cross-check only. Kept: fidelity standard, five goals, V1→V4 lineage; added scope note splitting built-but-inactive infra (SMP/IPI, full PAL trap delivery, SCSI/PCI enumeration) from live behavior.
- **Open / next:** User to choose next page — draft the **Memory** architecture page next, or first finish **Section 4 (Operating EmulatR)**. Remaining handbook pages to draft straight from V4 sources: Memory, Addressing, Firmware Decode & Decompression, Execution Flight Plan, plus deep-dive appendices.
- **Watch-outs:** H&M (Help & Manual) had the Overview file open and re-saved it mid-session, deadlocking the file-state harness (Read=unchanged vs Write=changed); resolved by deleting + rewriting fresh — if the page is open in an editor, reload it. Going forward, draft every page from V4 source + SRM/PALcode references, not secondary/Claude-web material. Note: the emulator boot blocker is unchanged and untouched — still **Task #3** `HW_MTPR 0x2D` DTB-double-miss spin (downstream of all completed FP/coverage work).
