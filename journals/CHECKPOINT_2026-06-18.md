# Checkpoints — 2026-06-18

## 15:07 — Chipset Titan/ALi work committed-pending; ES40 testing next
- **Working on:** Closing out the 2026-06-17 chipset session — Titan (21274) + ALi M1543C south-bridge interfaces and the Tsunami/Typhoon/Titan taxonomy correction. H&M reference topics for the chipset family were also formatted into `H&M/HMDocs/Topics/`.
- **Done since last checkpoint:** Titan scaffold (`chipsetLib/Titan21274_CsrSpec.h`, `TitanChipset.h`, `TitanPchip.h`) reusing Tsunami Cchip/Dchip/TIG with new dual G/A-port Pchip+AGP; `AliM1543C.h` ES40/ES45 south bridge with model-gated selection in `wireDevices`; `TsunamiVariant.h` adds Titan and maps DS15/DS25/ES45→Titan (was mislabeled Typhoon=21274); `Machine.cpp` builds chipset from ini model; es40/ds20 manifests runnable, ds25/es45 staged; verified standalone via g++. Memory.md updated. Seven H&M topics (Chipset, South Bridge, Cypress, CY82C693, Tsunami/Typhoon, DECchip 21274/Titan, ALi M1543C) formatted and cross-linked.
- **Open / next:** Git commit is still PENDING — blocked by a Windows-side `index.lock` in `EmulatRAppUniV4\Emulatr\.git\` that the sandbox could not remove ("Operation not permitted"). Staged set is the chipset/Titan/ALi/manifest/journal files (see 2026-06-17 session for the exact `git add` list + commit message). User must run the commit from the client holding the lock or delete the lock file. Note: Memory.md, H&M topics, REFERENCE_INDEX.md live OUTSIDE the `Emulatr/` git root and won't be in this commit.
- **Watch-outs:** (1) Linux mount served STALE cached copies of edited files this session — all writes validated via the Read tool (host view), not bash byte counts. (2) Per user: ES40 testing begins Thursday (today) — build DS10 first to confirm no regression, then watch the `HALTPROBE`/smir trace. (3) Titan/ALi verified standalone with g++ only; NOT yet built in MSVC/Qt.

## 23:07 — Disassembler-tooling decision: skip IDA, invest in Ghidra SLEIGH + objdump
- **Working on:** Session "d:\emulatr directory access" — advising whether to write an IDA processor module for DEC Alpha to cross-read the SRM firmware (`es40_v7_3.exe` and friends) alongside the existing Ghidra workflow. A `ds20` run is also grinding in the background toward the `P00>>>` prompt.
- **Done since last checkpoint:** Researched IDA's Alpha support and reached a firm recommendation. Findings: IDA *Free/Community* can't load custom processor modules at all (no SDK, no IDAPython processor API), and the stock Alpha module is *Pro-only* (IDA Home is x86/ARM/MIPS/PPC/RISC-V only) — so "write an IDA Alpha module" is blocked twice on the user's license. Stock Alpha module is also EV6-PAL-thin (renders `CALL_PAL`, `HW_MFPR`/`HW_MTPR`, `HW_LD`/`HW_ST`, `HW_RET` + BWX/CIX/MVI/FIX poorly). Verified in-sandbox that local `objdump` is x86-only and the Alpha backend can't be installed here (no root).
- **Open / next:** Recommendation delivered — (1) extend the **Ghidra SLEIGH** Alpha `.sinc` defs for EV6 PAL/privileged ops (free analog of an IDA processor module; reuses existing `ApplySrmSymbols.py` symbol-recovery work; user is evidently on a community SLEIGH Alpha module since stock Ghidra lacks Alpha); (2) add GNU **binutils `objdump -m alpha`** as a scripting/cross-check companion (`apt install binutils-multiarch`, disassemble raw container via `objdump -D -b binary -m alpha --adjust-vma=0x8000 <image>`); (3) three-way triangulation Ghidra ↔ objdump ↔ EmulatR decoder. Awaiting user's call on whether to return to the `ds20` run.
- **Watch-outs:** This thread is advisory/research only — no V4 source files changed. The PENDING git commit + `index.lock` blocker from the 15:07 entry is still unresolved and still needs the user to clear it client-side.

## 01:07 — DS20 boot wall reframed: kernel idle scheduler, dual-CPU rendezvous hypothesis
- **Working on:** Same session "d:\emulatr directory access" — now doing live analysis of where the DS20 SRM boot stalls, using a snapshot at `cycleCount=70,000,000,000` and Ghidra decompilation the user is pasting back.
- **Done since last checkpoint:** (1) Verified the snapshot parser against the host layout — little-endian header, `cycleCount` at file offset 28, `CpuState` POD at offset 300, `pc` at POD offset 592 → file offset 892; self-check passed (cycleCount read = 70e9, matches filename). (2) Established `0xb744` (RSCC PAL delay) was a **red herring** — it's just the delay subroutine. Across snapshots PC bounces over native `0x1ad6xx–0x1adbxx`, PAL `0xb74x`, and `0x7da7c` (DS20's shifted `timer_check`). (3) **Reframe:** firmware finished POST and is running the kernel idle scheduler in a loop — idle → timer_check (RSCC delay) → context-switch → idle — matching the SYSFAULT map `krn$_idle → timer_check → krn$_bpost`. This is the same idle state DS10 sat in just before the `>>>` breakthrough. (4) Decoded the pasted asm: `0x1adabc–0x1adb10` is the scheduler's **context-switch tail** (restores s0–s5, FP, GP, SP, ra, pv from a context block at `t0 = v0+8`); `FUN_001ae208` (called from `0x1ae200`) is the **dispatcher** — loads control object `t2=*v0`, fetches a method ptr `ra=*(pv+8)` via `*(t2+0x3c0)`/`*(t2+0x3c8)`, calls with `a0=0,t11=1`, jumps back to `0x1adabc`.
- **Open / next:** Awaiting decompiled C (Ghidra Decompiler pane, not listing) for three targets: (1) the idle-loop function enclosing **0x1ae200** — the run-queue/readiness condition that is the actual bug; (2) **timer_check at 0x7da7c** — what it polls/returns; (3) the prompt emitter — xref to string `P%02d>>>` at **0x164ba0** — to see what it blocks on before printing (semaphore / event / CPU-online check).
- **Watch-outs:** Leading hypothesis is a **secondary-CPU rendezvous** — DS20 is dual-processor but only `cpuCount=1` is modeled, so the boot CPU may idle forever waiting for CPU1 to hit a startup barrier; still need to confirm what CPU count DS20 actually reads (our HWRPB says 1 vs SROM / Tsunami CSR). Analysis/advisory only — no V4 source changed this thread. The PENDING git commit + `index.lock` blocker from the 15:07 entry remains unresolved (client-side fix needed). Linux mount can serve stale copies — journal written/verified via file tools, not bash.

## 01:4x — DS20 diagnosis closed + written up; conclusion = console-idle secondary-CPU stall
- **Working on:** Closing the DS20 boot analysis and persisting it.
- **Done since last checkpoint:** Pulled the rest of the picture from the user's Ghidra pastes: `timer_check` (`FUN_0007d9d8` + loop `0x7da68`) is a deadline wait that repeatedly calls scheduler-yield `FUN_001ad928` and compares `[GP+8]` (now) vs `[GP+0x10]` (limit); `0x164ba0` is **data** (console-var descriptor for `prompt`, default `P%02d>>>`), Ghidra mis-rendered as code. Checked V4: the HWRPB builder advertises `cpuCount=1` correctly (FirmwareDeviceManager.h), but the SRM self-builds its HWRPB from hardware probing and `ds20_platform.json`'s own comment warns "keep cpuCount=1 to avoid SRM secondary spin." **Firm conclusion:** DS20 reached console idle (krn$_idle healthy); the POST/init thread blocked (no POST strings printed) — most likely a secondary-CPU rendezvous; idle thread spins forever, shell never created, no `>>>`. Saved memory `project_ds20_console_idle_secondary_cpu_stall` + `reference_snapshot_pc_extraction` (+ MEMORY.md index); full write-up `journals/20260618_ds20_first_boot_console_idle.md`.
- **Open / next:** (1) **DS10 control run** to `>>>` on this same binary (`run_fw.sh ds10`, drop `--mem` → 1 GiB) — proves the stall is DS20-specific in one run. (2) **DS20 dual-CPU bring-up:** locate the init thread's secondary-presence/IPI wait and make the firmware see a true uniprocessor at that probe (or model CPU1 startup). Real task, not a patch. (3) `run_fw.sh` (in `out/build/release/` and `relwithdebinfo/`) seds `[System] model` per run + restores it; tees console to `fw_<name>_<ts>.out`.
- **Watch-outs:** Ghidra's Alpha decompiler is unreliable (asm/garbage C) — expect asm-level or dynamic tracing for the wait condition. DS20 image addresses do NOT map to DS10 (~14% identical; prompt at 0x164ba0 vs DS10 0x16fc10). The 70B auto-snapshots re-enter the same idle — snapshot once and iterate, don't re-grind. ES40 testing (the original Thursday goal) is still pending and needs the Titan/ALi MSVC build first.

## 02:07 — IPI self-test fix staged; run_fw.sh gains opt-in `cold` arg; DS20 cold-boot test running
- **Working on:** Same session "d:\emulatr directory access" — acting on the dual-CPU/IPI hypothesis from the 01:4x entry. An IPI fix (wiring the self-test against the Cchip) is edited but UNBUILT in MSVC, and `run_fw.sh` was restructured so a clean cold boot can be forced for the test. A `ds20 cold` run is being launched to see if POST advances past the boot wall.
- **Done since last checkpoint:** Rewrote `run_fw.sh` in BOTH `out/build/release/` and `out/build/relwithdebinfo/` to take an optional `cold` positional arg: `./run_fw.sh ds20 cold` passes `--no-autoload` and purges the stale `auto_*.axpsnap` (reclaims ~345 GB) for a genuine boot-from-firmware; bare `./run_fw.sh ds20` keeps the prior WARM default (autoload newest snapshot + autosnap). Extra Emulatr flags still pass through after the `cold` token (e.g. `cold --max-cycles 0x20000000`). Launch now also always clears a stale `EMULATR_STOP` sentinel so a leftover one can't instantly halt the new run. Verified the exec line (line 95, with `$COLD_FLAG`) and balanced `if/fi` via the host Read view; inline arg-parse test passed (`COLD=1` detected, passthrough intact).
- **Open / next:** Test sequence in flight: stop the still-running buggy process (`touch EMULATR_STOP`, await exit) → REBUILD in MSVC (the IPI edits are unbuilt) → `./run_fw.sh ds20 cold` → watch PuTTY for console output continuing PAST `Flash ROM writes are disabled` into the `eerom`/memory/`SROM` strings and on toward `P00>>>`. IPI self-test fires early in POST so the tell comes fast; a stall at the same spot is also a signal. User reported "running" — awaiting console tail / `fw_ds20_*.out` + PuTTY session log to judge whether the wall cleared.
- **Watch-outs:** IPI fix is edited but NOT yet built — results are meaningless until the MSVC rebuild lands. The `bash -n run_fw.sh` "unexpected EOF at line 67" was the known D:-mount phantom-truncation (sandbox served a stale partial copy right after the write), NOT a real script defect — confirmed complete via host Read. PENDING git commit + `index.lock` blocker from the 15:07 entry is still unresolved (needs client-side fix). ES40 testing still gated on the Titan/ALi MSVC build.

## 03:07 — DS20 root cause NAILED: `0xBFFC == 0xCAFEBEEF` secondary-CPU rendezvous; env-gated read-intercept staged for confirmation
- **Working on:** Same session "d:\emulatr directory access" — converted the dual-CPU *hypothesis* into a proven root cause via Ghidra decode, then landed a minimal confirmation hook. A `ds20 cold` run is in flight to validate.
- **Done since last checkpoint:** (1) Killed the last red herring — `FUN_001ad928` is NOT a yield/deschedule; it's `BIS a0,zero,t8` → `CALL_PAL 0x9d` (**RSCC**, confirmed `PAL_FUNC__RSCC=^x9D` in `ev6_vms_callpal.mar` → VMS personality) → `STQ v0,0(t8)` → `RET`. So `timer_check` is a **busy-delay spin**, not a sleep — the init thread is actively polling, not blocked/idle. (2) Decoded the poll `FUN_0007ecb0`: loads `v0 = *(int32*)0xBFFC`, compares against const `0xCAFEBEEF`, returns 1 (ready) / 2 (keep waiting). **Firmware spins forever waiting for PA `0xBFFC` to become `0xCAFEBEEF`** — a classic secondary-CPU "I'm alive" liveness flag. (3) Confirmed `0xCAFEBEEF` appears NOWHERE as a writer in the image (the `LDA -0x4004` address form occurs exactly once, at the read site) → nothing DS20 runs ever writes it → it's CPU1's handshake, and V4 models only CPU0. DS10 is single-socket → no rendezvous → reaches `>>>`. This vindicates the turn-1 dual-CPU instinct; the earlier IPI work is the *start* half, this `0xBFFC` flag is the *liveness* half that actually gates boot. (4) Landed an **env-gated read intercept** in the memory-load path (`applyLoadEffect`, alongside the existing TEMP load-watch on `br.data`, ~line 453/467–468 of the MemDrainer/load-effect header): when `EMULATR_CPU1_ALIVE=1`, a read of PA `0xBFFC` returns `0xCAFEBEEF`. Off by default; `std::getenv`/`<cstdlib>` already in scope so the patch is safe. Edit verified via Grep + host Read.
- **Open / next:** Confirmation run `EMULATR_CPU1_ALIVE=1 ./run_fw.sh ds20 cold` (rebuild first — header-only change recompiles dependents). **Success = DS20 prints PAST `Flash ROM writes are disabled`** into `eerom` / memory-sizing `Testing` POST and onward (a *new* downstream wall is expected and = real progress). User reports run still in flight; console so far only through `Flash ROM writes are disabled` (the pre-poll banner) — not yet conclusive. If it advances, replace the hack with the **proper fix**: model the secondary rendezvous — write `0xCAFEBEEF` to `0xBFFC` on absent CPU1's behalf at the actual secondary-start point (the "init-only pseudo-CPU" the user proposed turn 1), armed automatically without the env var. If it does NOT advance, re-check polarity/address by tracing the poll's caller.
- **Watch-outs:** Read-intercept is a CONFIRMATION hack, not the fix — and it requires the MSVC rebuild to mean anything (the edit is header-only but unbuilt at last report). PA `0xBFFC` is the *physical* address as decoded (`LDA -0x4004` form) — verify the load path sees the same PA after translation before trusting a negative result. PENDING git commit + `index.lock` blocker from the 15:07 entry still unresolved (client-side fix needed). ES40 testing still gated on the Titan/ALi MSVC build. D:-mount can serve stale copies — this entry written/verified via file tools, not bash byte counts.

## 03:3x — DS20 root cause CONFIRMED: rendezvous fix advanced the boot past the wall
- **Working on:** Validating the `0xBFFC == 0xCAFEBEEF` confirmation hook on a real cold run.
- **Done since last checkpoint:** Built + ran `EMULATR_CPU1_ALIVE=1 ./run_fw.sh ds20 cold`. **PROVEN:** new console line `Running on the ISP model.` (never present in any pre-fix run) appeared, and the PC advanced from the old `0x1ad9xx`/`0xb74x` rendezvous stall into new code at `0x78fxx` (cyc 1B — a *bounded* retry/countdown loop `LDL t0,[t2+0x10]; SUBL t0,1; STL; BGE` calling `FUN_001ad610/754`). So the secondary-CPU liveness rendezvous (boot CPU busy-polling PA `0xBFFC` for `0xCAFEBEEF`, never written because only CPU0 is modeled) IS the DS20 blocker — confirmed, not just hypothesized. Wrote it up: `journals/20260618_ds20_root_cause_cpu1_rendezvous.md`; memory `project_ds20_console_idle_secondary_cpu_stall` carries the full chain + `*** CONFIRMED ***`.
- **Open / next:** (1) Let DS20 keep running; watch PuTTY for `eerom`/memory/`Testing` POST and the next wall (another device/rendezvous poll, or eventually the VAX-float opcode-0x15 FP path). (2) Replace the env-gated read-hook with the **faithful** fix: write `0xCAFEBEEF` to `0xBFFC` once at the real secondary-start point (the "init-only pseudo-CPU" slot) so it arms without `EMULATR_CPU1_ALIVE` and the hook stops firing for every `0xBFFC` read. (3) Commit the real wins — Cchip IPI wiring (correct regardless) + the rendezvous fix once faithful.
- **Watch-outs:** The hook fires for ANY `0xBFFC` read forever — fine to confirm, NOT the final form. Expect more bring-up walls downstream; each is progress, not regression. PENDING git commit + `index.lock` still unresolved. ES40 testing still gated on the Titan/ALi MSVC build.
