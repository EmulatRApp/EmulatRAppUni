# Checkpoints — 2026-06-22

## 15:07 — Snapshot cadence + symbolication toolkit committed; no-warp profiler run staged for tomorrow
- **Working on:** Wrapping the night's work in session "Mount to d:\EmulatR" — landing the snapshot cadence change, drafting git commit text, and producing a self-contained diagnostic script for tomorrow's no-warp cold-boot profiler run on DS20.
- **Done since last checkpoint:**
  - `systemLib/Snapshot.h` — `kAutoSavePeriodCycles` changed 1B (`1000^3`) → 50B (`50 * 1000^3`); `kAutoSaveKeepCount` stays 5 (rolling net still covers 250B). Header constexpr → forces `Machine.cpp` recompile; no format/version change (`kCpuStateVersion` stays 10); does not affect manual `predig_` snapshots. Cuts deep-run periodic-save noise ~50×.
  - Symbolication toolkit verified/finalized under `tools/symbolication/`: `parse_symtab2.py` (correct parser — code entry = u32 at procedure-descriptor+8, VMS-style PDSC), `ds10_v7_3_symbols_entries.csv` (DS10 v7.3, 2832 funcs), `es45_v7_3_symbols_entries.csv` (ES45 v7.2/7.3, 2938 funcs), `apply_symbols.py` (Ghidra import @ base 0x8000), plus `extract_source_strings.py` and helpers. DS20/ES40 firmware are stripped → to be named via Ghidra VT/BSim from the DS10/ES45 reference maps.
  - Two-commit message drafted: (1) snapshot cadence, (2) symbolication toolkit. v1 artifacts flagged for deletion (`parse_symtab.py`, `ds10_v7_3_symbols.csv`, `ds20_v7_3_symbols.csv` — descriptor-addr, wrong).
  - `tools/diag_ds20_nowarp.sh` written: preflights build/firmware, explicitly `unset`s `EMULATR_IDLEWARP`/`EMULATR_RSCCWARP`/`EMULATR_TICKWARP` (warp off by default) + base-pin watch + TIG canary, pins `EMULATR_RETIRE_TRACE_DIR`, leaves `EMULATR_PLATFORM` unset (= ISP model, the working path), launches no-warp cold DS20 boot (no `--trace`, profiler always-on, 50B net).
- **Open / next:** Rebuild with 50B cadence and make the two commits. Tomorrow: run `bash tools/diag_ds20_nowarp.sh`; read top retire-PC buckets from the always-on `RetireProfiler` histogram; correlate the hot PC through the DS10/ES45 symbol maps (Ghidra VT/BSim) to name the boot wall.
- **Watch-outs:**
  - Do NOT `git add MemDrainer.h` — it still carries the temp START-WATCH (4 hits, marked REMOVE-BEFORE-COMMIT); strip or leave uncommitted.
  - Profiler dumps `profile_<ts>_run_end.txt` ONLY on a clean exit (`--max-cycles`, halt, or `EMULATR_STOP` sentinel) — Ctrl-C/kill produces NO histogram. Stop the run with `touch …/EMULATR_STOP`.
  - No-warp trades depth for ground truth: overnight covers fewer real cycles than the warped 108B did, so it may not reach the wall if the wall is deep — the early read (stall vs slow forward progress) is the value.

## 19:07 — Wall named (`krn$_poll`); pivot to "what is init waiting on" via surgical trace
- **Working on:** Same session "Mount to d:\EmulatR". Correlating the boot-wall histogram to firmware symbols, settling the symbolication strategy for the DS20 scope, and reframing next steps from broad symbolication to a surgical look at the init→poll handoff.
- **Done since last checkpoint:**
  - Built `wall_analysis_record.csv` — record of correspondence mapping each meaningful histogram bucket → DS10 symbol → apisrm source ref. Columns: `bucket_pc, pct, count, first_cyc, last_cyc, ds10_func, entry, offset, apisrm_file, def_line, def_kind, role_notes, confidence`. Two honesty columns: `def_kind` (C-def vs ASM/extern) and `confidence` (`exact-if-DS10 / candidate-if-DS20`).
  - Three buckets = 88% of post-banner cycles: `0x7b800`/35% → `krn$_poll` (entry `0x7b728`; body is **assembler** — `kernel.c:978` is only the `extern`, so Ghidra decompile, not source); `0x1c6400`/13% → `sysfault_nohalt` (no C def, ASM; decompile at `0x1c630c`; spans clock-ISR PC `0x1c6d4c`); `0xb400`/41% → unmapped PAL below console base `0x24000` (the wait/idle PAL primitive `krn$_poll` traps into).
  - Filled missed source refs via direct def grep: `reset_hardware → pc264_init.c:513`, `uptime → uptime.c:108`. `0x600xxx`/`0x900000` rows confirmed pre-relocation decompressor space (unmapped, ignore).
  - Settled symbolication: no external `.map` shipped; the artifact is the **in-image symtab** = the `0x17b220` table (ES45 sym array `0x1775f0`, name pool ~`0x17b220`), already cracked (record `{descriptor_ptr, name_ptr}`, entry `*(desc+8)`). One-pass import = `apply_symbols.py` (does its own `createFunction`, independent of Ghidra auto-analysis) on the **ES45/DS10** images at base `0x8000`. DS20 is stripped → named via VT/BSim from the named ES45/DS10 reference.
- **Open / next:** Pivot agreed — symbolication is solved; the wall is *named* (`krn$_poll` cooperative poll scheduler idling because foreground init is blocked, post-banner ~186.8M). Narrow question now: **what is the init thread waiting on?** Cheapest-first plan: (1) source read of `krn$_poll` poll-queue + what foreground init `krn$_sleep`s on + `ddb_startup`'s last driver-init poll; (2) surgical gated trace of the 185M–190M init→poll transition to name the last init fn + wait condition; (3) fix that device/event → init unblocks toward `start_secondaries`. Step 1 offered, awaiting user go.
- **Watch-outs:**
  - `krn$_poll` / `sysfault_nohalt` confidence is `exact-if-DS10 / candidate-if-DS20` — the 06-15 histogram's source model is unrecorded; names are cross-build candidates until confirmed on an ES45 run or via VT/BSim on DS20. Don't treat as exact-for-DS20 yet.
  - Rejected option: a Python re-implementation of the executor — EmulatR *is* the faithful generator; reimplementation would be slower, less correct, months of duplicated work. Keep using EmulatR-as-generator with gated/surgical traces, not broad ones.
  - hwrpb/ctb/memdesc are cold-boot step 5 (earlier than 186.8M, already built) — NOT the target of the init-wait trace; the wall is downstream of them.

## 21:06 — Probe-build no-warp DS20 run hit a NEW tight loop at `0x1adb60` (LL/SC spinlock); LL/SC rewire is prime suspect
- **Working on:** Same session "Mount to d:\EmulatR". Got the bringup-probes profiler build compiling under MSVC (vcvars issue solved), launched the clean-flash no-warp DS20 cold boot, and it parked in a sharp single-PC loop — pivoted from the broad `krn$_poll` wall to decoding/gating this tight loop.
- **Done since last checkpoint:**
  - Solved vcvars-in-bash: can't source a `.bat` into bash; instead run build inside one `cmd` that `call`s vcvars then cmake. Wrote reusable wrapper `tools/build_emulatr_diag.bat` (calls `…\2022\Community\VC\Auxiliary\Build\vcvars64.bat`, `cd /d` to `out/build/release`, reconfigures `-DEMULATR_BRINGUP_PROBES=ON`, builds Release). Run from bash via `cmd //c "…\build_emulatr_diag.bat"`. Flip `set "PROBES=OFF"` for perf builds.
  - Rebuilt with `EMULATR_BRINGUP_PROBES=ON` (profiler dump now actually compiled in) and ran `tools/diag_ds20_nowarp.sh` — clean DS20 flash, warp off. Run reached a **tight loop on a single PC `0x1adb60`** (much sharper than the prior broad `krn$_poll` spin).
  - Cross-build DS10 map **cannot name `0x1adb60`** — nearest symbol `tlist` is +0x13ac8 (~80 KB gap); this region is past where the DS10 map holds for DS20. Named by decode instead.
  - Decoded the loop from the image (`0x1adb60` is `0x8000`-based → file offset `0x1a5b60`): it's the tail of a **spinlock-protected routine** — `LDL_L r0,0(r22)` / `STL_C r1,0(r22)` / `BEQ r1,0x1adb14` (SC-fail retry) / `BNE r0,0x1adb14` (lock-held spin) → bump counter at `+32(r22)` → `STL r31,0(r22)` release → `RET (r26)` at `0x1adb60`. Recorded fault = DtbMiss.
  - Three hypotheses; prime suspect = **this session's LL/SC rewire** (MemDrainer → LockMonitor `STx_C` path): if `STL_C` now spuriously fails, `BEQ r1` retries forever. Behavior **changed shape after the rewire** (06-15 broad `krn$_poll` spin → now a tight `LDL_L/STL_C` spinlock); doctests exercise `LockMonitor` in isolation, not a live guest LL/SC with per-store `clearLine` + intervening PAL calls. Alts: (2) `kFaultDtbMiss` re-fault on the `(r22)` lock-word VA; (3) genuine deadlock (lock held, never released).
- **Open / next:** Staged the surgical gated trace to settle it — `EMULATR_GATE=open:0x1adb14,close:0x1adb60,rev:8` (format `open:..,close:..,rev:N`, `main.cpp:404`) over a cold DS20 boot; BreakpointSink dumps 8 full revolutions to `traces/<ts>_break.trc`. Read `r1` after the `STL_C` at `0x1adb28`: `r1==0` every rev on one CPU w/ no intervening store ⇒ rewire broke `STL_C`; `r0!=0` at `0x1adb30` ⇒ deadlock; DtbMiss in-window ⇒ TLB/mapping. Awaiting the `_break.trc` from the user.
- **Watch-outs:**
  - Profiler dumps `profile_<ts>_run_end.txt` ONLY on clean exit; stop with `touch …/EMULATR_STOP`, never Ctrl-C/kill.
  - `0x1adb60` is **unnamed** — DS10 cross-build map breaks down this high; do not trust a cross-build name here, decode/Ghidra-on-DS20 only.
  - If hypothesis #1 confirms, the bug is in our `MemDrainer` `STx_C` path (the LL/SC rewire) — fixable in our code, not deep firmware. This tight loop is a regression downstream of / distinct from the named `krn$_poll` wall.

## 23:06 — Pivot to PCF8584 IIC base provenance (DS20); store-watch landed, datasheet validates stub register model
- **Working on:** Same session "Mount to d:\EmulatR". Pinning the DS20 PCF8584 IIC controller base address so the firmware's `iic_init` traffic registers against `IicPcf8584Stub` instead of falling through `TsunamiPchip::write` to UNHANDLED — and confirming the stub's status-bit semantics against the datasheet before finalizing a model-conditional registration diff.
- **Done since last checkpoint:**
  - Confirmed PCF8584 register model from the datasheet: `A0` line selects **logic 0 → S0 (data/setup) @ +0**, **logic 1 → S1 (control/status) @ +1**, matching the log's observed `+0`/`+1` byte pairs exactly. Status (S1) bits pinned: `PIN`@7 (1=pending, 0=byte complete), `BER`@4, `AD0/LRB`@3 (master mode: 1=NACK=no slave on empty bus), `BB`@0. The poll waits `PIN→0` then reads `LRB=1` (NACK) on an empty bus.
  - Landed an env-gated IIC store-watch in `MemDrainer.h` (`EMULATR_IIC_WATCH`, mirrors the existing START-WATCH pattern, inserted right after that block) — logs storing `pc`, full guest `pa`, `memSize`, `memData` for the IIC writes that land at PA `0x800FFF80000`/`0x800FFF80001`. Verified ASCII-clean.
  - Drafted the model-conditional registration diff (`TsunamiChipset.h` ~636): IIC base `0xFFF80000` for DS20 (this run's provenance target) vs `0xFFFF0000` for DS10 (already proven), registered via `m_pchip.registerPciMemRange(iicBase, iicBase+2, &m_iic)`. DS10 path unchanged.
- **Open / next:** Run the base-pin: `EMULATR_FLASH_ROM=ds20_flash.rom EMULATR_IIC_WATCH=1 ./run_fw.sh ds20 cold --max-cycles 0x10000000` (needs the existing `EMULATR_BRINGUP_PROBES=ON` diag build), grep `^IIC-WATCH`, then disassemble around the storing PC to pin the exact `LDAH/LDA` forming `0xFFF80000` — proving the DS20 base the same way the DS10 `0xFFFF0000` was. Awaiting the watch output from the user (offered to disassemble the moment a few lines are pasted).
- **Watch-outs:**
  - Two open design decisions raised for user sign-off before finalizing: (1) key the IIC base on **model string `"DS20"`** vs a small **per-model platform table** (ES40/ES45/DS25 will each have their own base — a one-row-per-model table ages better than a DS10-vs-DS20 branch); (2) once the base is correct the stub sees DS20 `iic_init` traffic for the first time — must confirm `IicPcf8584Stub`'s S1 returns `PIN=0`/`LRB=1` (done/NACK) the way *this* image's poll expects, not just the DS10's.
  - `MemDrainer.h` now carries BOTH the temp START-WATCH (still marked REMOVE-BEFORE-COMMIT) and the new env-gated IIC-WATCH — keep both out of `git add` / strip before commit.
  - IIC base `0xFFF80000` is a target hypothesis, not yet proven — it is what the watch run is meant to confirm; do not treat as verified until the storing-PC disassembly pins the constant.

## 01:07 — MILESTONE: DS20 boots all the way to the `P00>>>` SRM console (banner → device enum → LFU `UPD>` → `update srm` → SRM prompt)
- **Working on:** Same session "Mount to d:\EmulatR". After landing the IIC base fix and the floppy/FDC edges, ran a long-budget cold DS20 boot. What earlier looked like a post-dva0 hang turned out to be slow-but-progressing firmware reaching interactive prompts — chased it through to the live SRM console.
- **Done since last checkpoint:**
  - **IIC base fix landed + DS20 device tree completes:** firmware banner now prints full enumeration — `probing hose 1/0 PCI`, `PCI-to-ISA bridge bus 1`, `bus 0 slot 5 -- dqa -- Cypress 82C693 IDE`, `initializing GCT/FRU at 3ff32000`. The `IicPcf8584Stub` registration stopped the `TsunamiPchip: UNHANDLED OUTER WRITE` fall-through for the IIC traffic.
  - **Floppy FDC 3-edge fix landed:** option-firmware scan of `dva0` (floppy) and `dqa1` (CD) now runs to completion — "Option firmware files were not found on CD or floppy" → drops into the LFU prompt as designed.
  - **Reached interactive LFU (Loadable Firmware Update) console `UPD>`** — the option-firmware scan found nothing on dqa1/dva0 and entered the firmware-update environment (Display/Exit/List/Readme/Update/Verify). Drove `update srm` → `Confirm…y` → `srm Updating to 7.3-1... Verifying 7.3-1... PASSED.` → `exit`.
  - **Reached the SRM console:** post-LFU reset → `Initializing…` → `Testing the System/Memory` (note benign `memtest: No such command`) → `AlphaPC 264DP 100 MHz Console V7.3-2, Feb 27 2007` → **`P00>>>` prompt, idle on console input.** This is the project's long-standing target end state for cold boot.
  - **Corrected the record:** the earlier "post-dva0 `krn$_poll` blocked-foreground wall" read was WRONG — that cooperative scheduler loop (`krn$_wait`/`krn$_semrelease`, DIVERT-REI idle at `0x1ad8f0`/`0x1adb60`) was the scheduler idling *between* the firmware's slow timed waits (floppy_spinup/`krn$_sleep`), not a deadlock. The big cycle budget proved it forward-progresses. Also flagged the GCT-truncation lead as a **red herring** (`ds20_platform.json` already has the working DS10 IIC devices; the `was=` DIVERT-REI register values jump randomly tick-to-tick = clock sampling, not a GCT walk; real GCT base `0x3ff32000` is above those pointers).
  - **`FCLOSE-WATCH` at `pc=0x5a278/0x5a2c4`** showed LFU closing the option-firmware files with valid high-mem `FILE*` (`0x3ff473xx`) — NOT `&spl_kernel=0x1cc4c0`, confirming the old fclose-corruption / DIVERT-REI is benign here.
  - Journals written this session (V4 tree): `20260622_ds20_iic_base_wall_cleared.md` (IIC milestone) + `20260622_pt2_floppy_fdc_and_post_dva0_frontier.md` (floppy 3-edge fix + corrected "it's the UPD> console, not a hang" finding). Memory wall record updated to end at "DS20 boots to a live SRM console."
- **Open / next:** Drive the live `P00>>>` SRM console — connect plink/PuTTY to **TCP 10023** and exercise SRM commands toward `boot`/secondary-CPU bringup. Per prior project notes the LFU→SRM path is now reproducible. **Snapshots work is now unblocked** (its deferral condition was "SRM reaches `>>>`" — that condition is met).
- **Watch-outs:**
  - **Uncommitted work owed before commit:** IIC base table + 3 floppy edges are uncommitted; **DS10 regression run (task #23) still owed** to prove the IIC/floppy changes didn't regress the working DS10 path. Pull the gated probes (`EMULATR_IIC_WATCH`, START-WATCH in `MemDrainer.h`, SDE swap-ledger) at commit — `MemDrainer.h` still carries temp watches marked REMOVE-BEFORE-COMMIT; do NOT `git add` it dirty.
  - Profiler dumps `profile_<ts>_run_end.txt` ONLY on clean exit; stop with `touch …/EMULATR_STOP`, never Ctrl-C/kill.
  - `set sys_serial_num` / `set sys_type` are unset (firmware noted "system serial number not set") — expected for a fresh flash; set before any config that depends on them.

## 03:07 — Post-milestone write-up: technical status update finalized + plain-language/LinkedIn announcements
- **Working on:** Same session "Mount to d:\EmulatR". Pivoted from boot engineering to communicating the DS20-to-`P00>>>` milestone — review/correct/finalize the engineer-facing status update, then derive non-technical and social variants.
- **Done since last checkpoint:**
  - Finalized **`journals/20260622_status_update.md`** ("final-final," ASCII-clean) after a review pass. Four corrections folded in: (1) removed LaTeX `$1000\%$` markup (rendered literal on GitHub) → plain "10x or more," framed as a *projection to be measured*, not benchmarked; (2) JIT reframed as a **different fidelity tier** — a functional fast-path validated against the interpreter at architectural-state checkpoints, not a cycle-for-cycle speedup; (3) `show all` (not a real SRM command) corrected to `show config`/`show dev`/bare `show`, with `help`/`memtest` "No such command" gaps kept in the cosmetic-nits bucket; (4) `get_sysvar` line now reads "default member id → DSRDB name lookup" rather than "default struct member."
  - Doc content confirmed accurate on the two deferred **cosmetic identity quirks**: ISP-model path (`platform()` returns `ISP_MODEL` when magic `0xCAFEBEEF` is at `0xBFFC`, else `REAL_HW`) and the "AlphaPC 264DP 100 MHz" banner (shared dual-21264 Tsunami 264DP reference design; chassis disambiguation needs the unmodeled IIC OCP probe) — presented as two **independent** mechanisms, not one cause. DS10 names itself correctly (distinct system type, no chassis probe).
  - Wrote a **plain-language announcement** (no hex/chipset jargon) alongside the technical doc — careful to say it reaches the **firmware console**, not "runs the OS yet" (no overclaim ahead of OS-boot work). Plus a short social/blurb version, a one-liner, and a **low-key LinkedIn** progress update (honest "reaches console, not OS"; suggested restrained tags `#retrocomputing #emulation #DECAlpha` or none). User approved ("perfect").
- **Open / next:** Back to engineering when resumed — the queue is unchanged from the 01:07 milestone: commit the IIC base table + 3 floppy interrupt-edge fixes, run the **DS10 regression (task #23)**, mint the model-tagged `P00>>>` snapshot (**task #25**, now unblocked since SRM reached `>>>`), then OS boot (Tru64/OpenVMS) is the next frontier. Next week's deliverable: zipped ready-to-run Windows binaries.
- **Watch-outs:**
  - This was a docs/comms session — **no code committed**; the uncommitted-work debt from 01:07 still stands: IIC + floppy fixes uncommitted, DS10 regression still owed, and `MemDrainer.h` still carries temp watches marked REMOVE-BEFORE-COMMIT (do NOT `git add` it dirty).
  - Status doc claims to verify against: "1000%/10x" JIT speedup is **projected, not measured** (no JIT exists yet); ES40's ALi M1543C is modeled but may be partial for ES40's needs ("bridge swap + finish what ES40 exercises").

## 05:07 — DS20 `P00>>>` snapshot minted + warm-resume proven (15s); pivot to user-manual TOC review
- **Working on:** Same session "Mount to d:\EmulatR". Capturing the `P00>>>` baseline as a snapshot, proving warm resume, then reviewing the EmulatR user-manual structure (`table_of_contents.xml`, Help & Manual) for ordering/scope and slotting in new reference topics.
- **Done since last checkpoint:**
  - **Cold-boot timing milestone:** DS20 now cold-boots to `P00>>>` in **~1.5 min** (down from infinite-hang a week ago, and from ~9B cyc earlier) — fixes compounded: IIC unblock + floppy fast-enumerate + `memory_test none` (skips POST) + clean PROBES-off build.
  - **`P00>>>` snapshot minted:** `EMULATR_CONSOLE_SNAPSHOT=1` + `set oem_string snapshot` fired the marker → `Snapshot::save: wrote 4299128339 bytes to snapshots\predig_oemsnap_cyc2022094226.axpsnap (cycle=2022094226)`. Full 4 GB machine state at the SRM prompt. Advised rename to model-tagged `predig_ds20_p00prompt_cyc2022094226.axpsnap` (the no-model name is exactly the autoload-safety gap task #25 closes).
  - **Warm resume proven end-to-end:** no-`cold` relaunch landed at `P00>>>` in **~15s** (~6× the 1.5-min cold path; the 15s is mostly reading/reconstructing the 4.3 GB image). `show memory` = 4096 MB / 0 bad pages; `show` env faithfully restored (`sys_serial_num test1234`, `oem_string snapshot`). Snapshot confirmed a complete, correct baseline — **Snapshots deferral condition (SRM reaches `>>>`) is now fully met.**
  - **Pivot to docs:** read the manual TOC; gave a structured review. Concrete fix called out: §6 Reference ordering — `Command-Line-Options` + `EmulatR-Subsystems` currently land *after* "Attributions & Credits"; regroup as Command Line Options → Environment Variables → (Logging) Subsystems → Glossary → Dependencies → Credits-last. Plus: scope "EmulatR Subsystems" to logging (cross-link §4.8), de-dupe Build/CMake (§2 vs §8.4 vs §8), move "82077 Controller" under §5 Architecture→Chipset, hide authoring-scratch topics ("Stylesheet for Style Test", "Example styles."), and name §5-vs-§8.3 overlaps "(Overview)" vs "Internals".
- **Open / next:** User observation to act on: many manual topics are **brief overviews only** given how much detail now exists — likely next step is fleshing out thin reference sections (Command-Line-Options, Environment Variables, Subsystems) from the real source. Engineering queue unchanged: commit IIC base table + 3 floppy edges, run DS10 regression (#23), formalize snapshot tooling (#25, now unblocked), then OS boot (Tru64/OpenVMS). Next week's deliverable: zipped ready-to-run Windows binaries.
- **Watch-outs:**
  - **`memory_test full`, not `none`, in this flash/snapshot** — the earlier `set memory_test none` never persisted because env only flushes to `ds20_flash.rom` on a clean stop (`EMULATR_STOP` sentinel); an unclean exit silently drops env changes. Doesn't affect warm resume (skips POST) — only matters for cold boots. New task created for the NVRAM-persistence behavior; user confirmed they did stop via `EMULATR_STOP`, so the drop predates that. Workflow rule: do `set`s, then exit via `EMULATR_STOP` (or re-mint) so NVRAM actually persists.
  - Snapshot is named `predig_oemsnap_…` with **no model tag** — rename before relying on it so it can't autoload into a DS10 run.
  - Still-uncommitted debt from 01:07/03:07 stands: IIC + floppy fixes uncommitted, DS10 regression owed, `MemDrainer.h` carries temp watches (do NOT `git add` dirty).
