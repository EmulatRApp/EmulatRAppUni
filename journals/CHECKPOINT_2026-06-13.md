# Checkpoints — 2026-06-13

## 19:39 — Session resumed; context loaded, picking next frontier (PCI #41 / dq boot / mem-size)
- **Working on:** Fresh session (`local_37375327`) re-attached to `D:\EmulatR`, reloaded context from `Memory.MD`. No new code yet — at the "what to pick up" decision point for the day, between three candidate threads.
- **Done since last checkpoint:** Nothing new in V4 source this session (access + context refresh only). Prior EOD (2026-06-12, session `local_1ba774b1`) landed and committed Phase A of the `IBlockMedia` byte-sourcing seam (#33) — `deviceLib/scsi/{IBlockMedia.h,FileBlockMedia.h,BlockMediaFactory.h}`, ATAPI READ path (#31), OS-suffixed manifests (`.win`/`.linux`), `create_if_missing` auto-provisioning, `tools/mkdisk.py`. Suite 449→**454 / 5999 / 0**. Git check-in comment was drafted that session.
- **Open / next:** Choose among (a) **#41 "PCI COMPLETE"** scaffolding — trace-first: capture `EMULATR_PCI_CFG_TRACE`, add `IPciDevice` config/BAR seam, fix dynamic BAR→range rebind (B3/#32), enumerate DE500 tulip without perturbing the GCT; hard-dep for #39 SYSBOOT> scaffold. (b) **dq boot ACCEPTANCE trace** — `boot dqa1` (Tru64 installer) → install onto `dqa0.img` → `boot dqa0`. (c) **Confirm 64→1024 MB fix is live.** Stated plan from prior session: begin #41 PCI work.
- **Watch-outs:** Last RelWithDebInfo run still printed `64 Meg` (stale binary / fallback not engaged). Verify with `EMULATR_NO_AUTOLOAD=1` cold boot: stderr ini line, `1024 Meg` banner, `show config`/`show memory` → `Array 0: 1024 MB`, `AAR0=0x7009`; then re-mint snapshots (old ones bake in 64 MB). Also pending at `>>>`: `show device` for dqa0 + a boot-time metric. Reconciliation: fold B-track items (B1 PCICFG trace #30, B2 IPciDevice #31, B3 BAR rebind #32, #7 DE500 #8) under the #41 umbrella so there's one source of truth.

## 21:06 — Launch-shell firmware-path hardening + synthetic disk catalog (16 GB–1 TB)
- **Working on:** Same session (`local_37375327`). Two concrete deliverables landed after the 19:39 decision point: (1) made `launch_vms_boot.sh` firmware-self-sufficient, and (2) drafted seven synthetic ATA disk-catalog entries for `config/disk_types.json`.
- **Done since last checkpoint:** (1) **`launch_vms_boot.sh` rewritten** — firmware now located two ways: shell passes `--firmware firmware/ds10_v7_3.exe` explicitly (CLI wins over ini) AND ini fallback `[ROM] firmwareImage = firmware/ds10_v7_3.exe`; both resolve to the CMake POST_BUILD copy at `build/firmware/ds10_v7_3.exe`. Script `cd`s to its own `$RUN_DIR` (anchored, not hardcoded CWD) and preflights that the firmware file exists before launch so a missing ROM fails loud instead of dying in SrmLoader. Initial `Write` truncated at line 49 (known cowork-Write-on-D: truncation gotcha) → rewritten via bash heredoc, now 59 lines, syntax-clean, firmware verified resolvable. (2) **Disk catalog** — verified existing pattern (`EMULATR-DISK-4G/-9G` are `transport:"ata"`, binary GiB labeled "GB", `total_sectors` authoritative) and computed exact sector counts for 16G(33554432)/32G(67108864)/64G(134217728)/128G(268435456)/256G(536870912)/512G(1073741824)/1T(2147483648), all `transport:"ata"`. Snippet handed to user, not yet merged.
- **Open / next:** User to choose disk-catalog merge mode: (a) merge all seven ATA entries into `config/disk_types.json` + the two build copies, (b) split ≥256 GB to SCSI (`mscp_media_class:"DK"`) for the future `dka` path, or (c) leave as snippet. Then re-run `./launch_vms_boot.sh`; SRM should load → wait for `>>>`, `show memory`, `boot dqa1`, send `boot_vms_*.log`.
- **Watch-outs:** Two real hardware walls inside the disk range — **ATA 28-bit LBA**: 128 GiB lands exactly on 2^28 (one past 28-bit max), 256 GiB+ need 48-bit LBA (ATA-6); DS10's Cypress 82C693 (`Cy82C693Ide`) LBA48 support unconfirmed, so big ATA sizes may be unreachable on the `dqa` path. **OpenVMS ODS-2**: 1 TiB = 2^31 blocks, one past the signed-31-bit block count older VMS assumes. Design debt #42: program resolves ini relative paths against CWD, not `applicationDirPath()` — the shell's `cd` masks it today but robust fix is to resolve QSettings paths via `applicationDirPath()`.

## 23:06 — Tsunami halt-register blocker closed (code + doctest + inline docs + journal + citations)
- **Working on:** Same session (`local_37375327`). Finished and fully documented the TIG halt-register fix (#5) that was blocking the boot past the halt gate.
- **Done since last checkpoint:** (1) **Halt register fix stands as implemented** — CPU0 `0x3C0` / CPU1 `0x5C0` now read `0x0000000000000000`, writes absorbed; rationale documented in `TsunamiChipset.h:679-687` (two registers are the complete, faithful surface for the Tsunami TIG halt line — no register-count change needed). (2) **Citations added** — `TsunamiChipset.h:703-712` now carries a path-qualified `References (authoritative)` block (PALcode `ev6_osf_pc264_pal.mar` lines 1264-1289 = TIG Register Usage map; AXPBox `src\System.cpp` reset+read-decode; journal; memory note). Journal's `References` section expanded with full paths + line numbers (apisrm `ev6_osf_pc264_pal.mar`, `boot.c`, `pc264.c`, `generic_messages.c`, `stub_halt_switch_in.c`; AXPBox `System.cpp`; EmulatR fix/test/memory artifacts). All blocks ASCII-clean. (3) Task #5 marked done. (4) **`build_test_run.sh`** written to run dir — builds ninja config, runs the halt-register doctest (`ISystemBus arbiter` case), and on green hands off to `launch_vms_boot.sh`. Syntax-checked, ASCII-clean.
- **Open / next:** Build + run must happen on the user's Windows box (sandbox is Linux, no MSVC, can't run `Emulatr.exe`). Path: rebuild → chipset suite → `launch_vms_boot.sh` → at `>>>` confirm `e pmem:801300003C0` reads `0000000000000000` → `b dqa1`. Send back the `boot_vms_*.log` to pin where OpenVMS stops.
- **Watch-outs:** This is the first boot to proceed past the halt gate, so expect to trip 1-2 *new* blockers before the anticipated VAX-float wall (opcode 0x15, ticket #43) — likely the dqa0 enumeration gap or PCI depth during VMB. Normal forward progress; each is a quick pin from the log.

## 01:06 — Console-armed retire-trace instrument to root-cause the halt read (MMIO vs DRAM impure flag)
- **Working on:** Same session (`local_37375327`). Built a "gate all possibilities" tracing harness so the next boot answers in one shot whether the SRM's halt read is a chipset MMIO register or the impure-DRAM flag.
- **Done since last checkpoint:** Three code hooks across `chipsetLib/TsunamiChipset.{h,cpp}` + `main.cpp`. (1) **Console-armed trace trigger** — reserved TIG PA `0x801_3000_0FF8` (`kTigTraceArmReg`); a console **read** (`e pmem:80130000FF8`) turns the retire trace **on until run end**, a **deposit** (`d pmem:80130000FF8 N`) traces N instrs (`0`=off); both route through the chipset read/write path. (2) **Auto HALTPROBE** — any non-zero read in the TIG window `0x801_3000_0xxx` logs `pa`/`width`/`value` to the console, so the halt register announces itself even un-armed. (3) **Window-only sink** — `EMULATR_TRACE_WINDOW=1` opens `_srm.trc` but emits *only* while armed, so cold boot doesn't produce a multi-GB stream. `trace_halt.sh` written to the run dir for the armed flow.
- **Open / next:** Build on the Windows box (IDE or `build_test_run.sh`) → run `./trace_halt.sh` → at `>>>`: `e pmem:80130000FF8` (examine should show `7FFFFFFFFFFFFFFF` = armed) → `b dqa1` (only this streams to `_srm.trc`). Send back the HALTPROBE lines (if any) + the `_srm.trc` tail around the `Halt Button is IN` emission. Decode: HALTPROBE **fires** → halt read is MMIO, line gives exact PA+value to model; HALTPROBE **silent** → it's the DRAM impure flag, and `_srm.trc`'s `HW_LD a=<pa> v=<value>` pins the address.
- **Watch-outs:** D: mount staleness bit again — a bash `grep` reported the write hook "didn't land" (only one `kTigTraceArmReg` in the cpp), but the host-authoritative **Read** confirmed both hooks present and well-formed (read trigger + HALTPROBE at `TsunamiChipset.cpp` 131-159, write control + halt-clear at 229-242). Trust Read over bash mount/`awk` brace-deltas on D:. Keep `_srm.trc` window-gated — un-gating floods the cold boot.

## 03:06 — Run-helper scripts made CMake-wipe-proof; memory.md + journals updated; boot run armed
- **Working on:** Same session (`local_37375327`). Hardened the trace/boot tooling against the recurring "CMake regen wiped my scripts" failure, then updated the day's memory/journal record. User is mid cold-boot run via `./tools/trace_halt.sh`.
- **Done since last checkpoint:** (1) **Run helpers moved into source + auto-deployed** — `trace_halt.sh`, `launch_vms_boot.sh`, `build_test_run.sh` now live in `Emulatr/tools/` and are listed in `CMakeLists.txt` `EMULATR_TOOL_FILES`, so the existing `[Emulatr] copy tool` POST_BUILD re-copies them to `run-dir/tools/` on every build — a cmake cache regen can no longer delete them (they regenerate from source). All three re-anchored to the run-dir root (`../`) since they now sit one level down in `tools/`, so `./Emulatr.exe`, `firmware/`, and the manifest still resolve. (2) `trace_halt.sh` also now sets `EMULATR_TIG_TRACE=1` (unmodeled-TIG canary) and points the operator at `e pmem:80130000040` → expect smir = 0. (3) Dropped fresh copies directly into both build dirs' `tools/` so the user can run **immediately** without waiting for the next build. (4) **`memory.md`** got a new `## Current state -- 2026-06-13` section (full arc: OpenVMS wired → mem-fix-live + dqa0 enumerating → firmware-path fix → halt-gate hunt → smir → faithful `TsunamiTig` → tooling-persistence fix) and both of today's journals added to the "Recent journals" table; the EOD/TIG journals got the run-helper/CMake addendum appended.
- **Open / next:** User is running `./tools/trace_halt.sh` (sufficient on its own — arms `EMULATR_TRACE_WINDOW=1` quiet + `EMULATR_TIG_TRACE=1`, tees to console log). At `>>>`: `e pmem:80130000040` should read smir = **0** (fix verification), then `b dqa1` should cross past the halt gate into VMS bootstrap. Awaiting the console-log tail after `b dqa1` to pin where boot next stops.
- **Watch-outs:** Windows copy may not preserve the Unix exec bit, so `./tools/...` can fail → use `bash tools/trace_halt.sh`. A stray `scripts/trace_halt.sh` was created earlier and couldn't be deleted from the sandbox — inert, not wired into CMake, `rm` when convenient. Old root-level script copies are superseded and will vanish on next cmake regen. Expect 1-2 *new* blockers after the gate before the anticipated VAX-float wall (opcode 0x15, #43).

## 05:06 — `b dqa1` still refused; canary pins halt to `clr_irq4` (TIG+0x440) — halt is an uncleared IRQ4, not smir (EOD)
- **Working on:** Same session (`local_37375327`). Ran the armed trace boot. smir verification passed but `boot` still refused; the `EMULATR_TIG_TRACE` canary identified the real halt mechanism.
- **Done since last checkpoint:** (1) **smir fix confirmed live** — `e pmem:80130000040` reads **0** (was `0xFFFFFFFF`); register-level fix works. (2) **`b dqa1` still prints `Halt Button is IN, BOOT NOT POSSIBLE`** (two attempts) — so the boot halt check reads more than smir. (3) **Canary found the thread:** firmware does repeated **`clr_irq4` writes at TIG+0x440 (`pa=0x80130000440`)** (`EMULATR_TIG_TRACE: unmodeled TIG WRITE pa=0x80130000440 off=0x440`, log lines 142025/142026/147065/147066). On PC264 IRQ4 is the halt line → firmware sees the halt interrupt pending, tries to clear it, our model no-ops the clear → halt stays asserted = "Halt Button is IN." smir reading 0 was necessary but the halt is delivered as an **uncleerable interrupt**, not a status read. (4) Task #5 updated with the `clr_irq4`/IRQ4 finding.
- **Open / next:** Model the **`clr_irq*` family (TIG `0x400`–`0x500`)** to actually clear the matching interrupt source, and find what leaves **IRQ4 (halt) asserted** — strong bet a **Cchip DRIR/DIR bit at `0x801_A000_0300` / `0x280`**, *outside* the TIG window the canary covers. Quick confirm before coding: `e pmem:801a0000300` at `>>>`. This is next-session work.
- **Watch-outs:** **Retire-trace capture never fired** — the `_srm.trc` is header-only (7 lines, stale 20:59:55 timestamp) because the run lacked `EMULATR_TRACE_WINDOW=1` (no sink existed to arm). Capture only works via `tools/trace_halt.sh` (or explicit `EMULATR_TRACE_WINDOW=1`). Arm with a **READ** of `pmem:80130000FF8` (deposit = disarm/count); stop with `touch …/EMULATR_STOP`; trace flushes only on clean exit (`~Machine`→`~DecListingSink`), so mid-run "empty log" is expected. Clean EOD stopping point — lead is concrete, not a dead end.
