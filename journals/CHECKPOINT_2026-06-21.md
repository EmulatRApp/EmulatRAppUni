# Checkpoints — 2026-06-21

## 15:06 — Linux/ARM platform-config variants + manual-ZIP packaging
- **Working on:** Producing host-specific platform manifests for the release build (Mint/x86-Linux and ARM), and settling on a manual distribution path after CPack proved unreliable.
- **Done since last checkpoint:** Wrote 6 variants in `out/build/release/tools/` — `ds10_platform.{ux,arm}`, `ds20_platform.{ux,arm}`, `es40_platform.{ux,arm}`; all validated as well-formed JSON (correct platform + 3 PCI devices each). Only host-specific content was DS10's media paths: ISO POSIX-ified to bare `alpha082.iso` (resolves against `[Storage] diskDir`) and host-optical passthrough changed from `D:\isos\...` / `host:0` / `\\.\E:` to `/dev/sr0`. DS20/ES40 ship empty media (`""`), so their variants are topology-identical to `.win` apart from a host marker comment. `.ux` and `.arm` are intentionally identical today (x86-Linux and ARM share `/dev/sr0` + POSIX paths); `.arm` kept as a reserved slot for future divergence.
- **Open / next:** User opted to build the installable distribution ZIP manually rather than fix CPack. Manual runnable set = `Emulatr.exe` + windeployqt-staged Qt DLLs + `EmulatrV4.ini` (curated DS10 default) + relevant `*_platform.*` manifest + `LICENSE.md`. No further code action requested.
- **Watch-outs:** The 6 variants live in a **build-output** dir — a clean rebuild won't recreate them (CMake only copies `.win`/`.linux`). Also the runtime suffix logic (Machine, CMakeLists ~line 838) selects `.win` on Windows / `.linux` elsewhere — it does NOT look for `.ux`/`.arm`, so to boot from them today you must rename to `.linux` per host or point `EMULATR_PLATFORM_CONFIG` at the exact file. Wiring real `.ux`/`.arm` suffix selection + canonical source placement + CMake copy rules was offered but deferred (user said "It is fine").

## 21:06 — SMP CPU-start TIG latch + clean-room `Ev6Pc264PalDefs.h`
- **Working on:** Modeling the per-CPU CPU-START path and formalizing PC264 PAL impure-region offsets as named constants. Two changes landing as one: (1) a new clean-room header `coreLib/Ev6Pc264PalDefs.h` mirroring `Processor Support\Palcode\palcode\srmconsole\EV6_PC264_PAL_DEFS.SDL`; (2) CPU-start latch semantics in `chipsetLib/TsunamiTig.h`. Session approved by user ("Let's do it"); assistant searching for existing hardcoded offsets so the new constants get **consumed**, not just defined.
- **Done since last checkpoint:** Verified the decode: TIG `xtig()` maps `PA = 0x801_0000_0000 + (offset<<6)`, so `outtig(0xC00028+id)` lands at `0x80130000A00` = `kIpcr0` — i.e. the existing `kIpcr0..3` (0xA00/A40/A80/AC0) registers ARE the per-CPU CPU-start registers (the old `TODO(SMP)`/"ipcr" label was a misnomer; the **write event** at value 0 is the kick, not an IPI). Confirmed from `EV6_PC264_PAL_DEFS.SDL`: impure start slots `PAL$CPU0_START_BASE=0x100`, `PAL$CPU1_START_BASE=0x108` (contiguous 8-byte), plus `PAL$PRIMARY=0x200`, `PAL$CALLBACK=0x210`, `PAL$HALT_SWITCH_IN=0x220`. Clarified the halt gate has two independent arms: the **hardware** smir (TIG+0x40, the resolved 2026-06-13 blocker, now `0`) vs. the **software** flag `PAL$HALT_SWITCH_IN` (impure +0x220) set only on a real HALT IRQ<4> by `sys__int_hlt` and read-and-cleared by `halt_switch_in()` (`pc264.c:1693`); not a live blocker since PAL zeroes it at init and we inject no front-panel halt.
- **Open / next:** Apply the combined diff. Planned `TsunamiTig.h` edits: relabel the kIpcr comment to "per-CPU CPU-START", add `uint32_t m_cpuStartReq` latch set on `kIpcr0..3` writes, clear in `reset()`, add `&& m_cpuStartReq==0` to `isAtResetState()`, and public API `cpuStartRequested(id)`/`clearCpuStartRequest(id)`/`pendingCpuStartMask()` (bounds-checked 0..3). New `Ev6Pc264PalDefs.h` modeled on existing `coreLib/Ev6EntryVectors.h` provenance/license. Add a TIG unit test (latch on `kIpcr1`, clear, `isAtResetState()` toggles). User owns the final MSVC build + test run + commit (sandbox can't build).
- **Watch-outs:** Constants are **offsets, not addresses** — linkage fields resolve against the PAL/linkage base (from runtime `palBase`/SRM load PA), not a fixed address; header must document which base each group uses (verify-before-decode), like `Ev6EntryVectors.h`. Do NOT duplicate CSERVE codes — reuse existing `PalEntries`/`execCserve` SSOT; SDL `$cserve_def` is cross-check only. EmulatR must never write `HALT_SWITCH_IN` directly — model the front-panel halt via the hardware smir/IRQ<4> event and let PAL set the flag. Change is asserted byte-identical on current UP DS20 boot (only primary in `cpu_enabled`, so `kIpcr` never written, latch stays 0) — pending user build to confirm.

## 23:07 — Empirically pinning the CPU1 start-slot PA (value-watch, not derived base)
- **Working on:** Locating where `start_secondary` stages the secondary CPU's PAL entry, to anchor the SMP CPU-start path. Pivoted from an address-keyed watch to a **value-keyed** START-WATCH because the linkage base can't be derived in closed form.
- **Done since last checkpoint:** Ran cold DS20 (`fw_ds20_20260621-145315.out`); pre-run dump line 34 gives ground truth `palBase=0x900000`, `pc=0x900001` (palBase|1, enter-in-PAL), OS target `0x600000`. Address-keyed START-WATCH on PA `0x100..0x120` **never fired** despite hitting the 268M cycle cap (`MaxCyclesExceeded`, cycles=268435456 — well past ~190M powerup, so `start_secondaries` ran) ⇒ **base-0 candidate FALSIFIED**, and the briefing's "candidate base = PA 0x0" premise was wrong (it came from the wrong SrmLoader path). Established correct loader model: this run uses the **stub loader** (`SrmLoader` lines 215–243), `loadPa=0x900000`, PA `[0..0x900000)` zero-init — NOT the verbatim-to-PA-0 path (line 262). Key clarification from user: **we are reading the DECOMPRESSED image, not the packed `.exe`**, so ghidra addresses correspond to runtime layout (single constant offset per relocation epoch).
- **Open / next:** Swap Diff 1 to the **value-watch** in the main step loop (catch the 8-byte store by `r.memData == 0x900001` / fallbacks `0x600001` / `0x8001` — the three palBase relocation epochs), rerun cold at 268M with `EMULATR_START_WATCH=1`. The hit **paired with the TIG `0xC00029` write** is `start_secondary`'s slot store; captured `pa` is the empirical answer. Model: `runtime_PA(x) = x + palBase − 0x8000`, predicting CPU1 slot `0x108 + palBase − 0x8000` = **`0x8F8108`** (epoch palBase=0x900000) or **`0x5F8108`** (epoch 0x600000). Value+PA cross-confirm in one shot; also locks ghidra→runtime delta `B_rt = palBase − 0x8000`.
- **Watch-outs:** Do NOT bake any derived base — `palBase` relocates during boot (`0x900000`→`0x600000`→`0x8000`), so "the base" is not constant; measure it. Earlier `0x90001`/`0x90000` ghidra reads were red herrings (`LDAH 0x9` = 0x90000, an ordinary mid-function `LDL`, an order of magnitude below runtime `palBase=0x900000`); ghidra load base ≠ runtime PA until `B_rt` is pinned. One open question to record the delta correctly: is ghidra's image based at `0x0` or `0x2000`? Workspace shell dropped mid-check — logs were read via host file tools, not bash byte counts.

## 03:07 — Recovering the embedded SYM symbol table for profiler symbolication
- **Working on:** Building a runtime `name→address` map so the profiler's hot PCs can be named (immediate goal: see *why* `ddb_startup` device-init never ran). Started down a source string-anchoring path, then discovered the firmware carries its own symbol table and pivoted to recovering that directly.
- **Done since last checkpoint:** (1) Built the string-anchoring pipeline as plan A — `extract_source_strings.py` → `source_strings.csv` yields **5,548 unique strings across 1,447 string-bearing functions; 1,230 functions uniquely seedable by a single string**. Relevance confirmed: `ddb_startup`/`powerup`/`platform` all seedable, and `ddb_startup`'s strings are literally `"calling module %s\n"` / `"%08X exit status for %s_init\n"` (the device-init trace) — the 108-byte console shows **none** of them, independently reconfirming `ddb_startup` was never reached. Staged `dump_ghidra_strings.py`, `join_symbols.py`, `apply_renames.py`. (2) Ruled out building apisrm: 336 files of DEC C for OSF/Alpha + MACRO-64 + firmware linker (no DEC toolchain in sandbox), and a rebuild's addresses wouldn't match the shipped `ds20_v7_3.exe` anyway. (3) **Breakthrough:** the decompressed image contains an **embedded symbol table** — 145/200 sampled function names present as strings (`start_secondary`, `powerup`, `halt_switch_in`, `ddb_startup_driver`…). Source-documented, no blind reversing: `sym_def.h` defines `struct SYM { unsigned int value; char *name; }`; `build_symtab.awk`/`bldsym.awk` bake the DEC link map into the firmware at DEC's build time; `symbols.c`/`sym_driver.c` do runtime lookup. Layout = array of `{uint32 address, name-ptr}` + a variable-length, 8-byte-aligned **name pool** (the 4-byte fields are why the earlier 8-byte address scan found nothing). ~72% of function names already confirmed present — exact runtime addresses, same toolchain/layout as what executes.
- **Open / next:** Plan pivots to **parse the embedded SYM table out of the image** → `ds20_symbols.csv` (`address,name`) → runtime resolver names the profiler PC. Have the DS10 decompressed image on hand to validate the parser (DS10/DS20 share the apisrm format); for the real map need the **decompressed DS20 image** (run `ds20_v7_3.exe` through the host-decompressor oracle, or export bytes from DS20 Ghidra). Assistant offered to write + validate the SYM parser against DS10 now (prove recovered `name→address` against known functions) so it's ready the moment the DS20 image lands — awaiting user go-ahead / the DS20 image.
- **Watch-outs:** `build_symtab.awk` supports include/exclude rules ("custom and small" tables) — the embedded table **may be curated/sparse**; keep the string-anchoring pipeline as fallback, and the user's Alpha box in reserve *only* if a complete name list is needed beyond what the table carries. Do NOT use a rebuilt link map for runtime PCs — a fresh build relocates to different addresses than the shipped image; the whole point is the table carries the **exact shipped addresses**. Parser must read 4-byte (not 8-byte) record fields.

## 05:00 — DS10 symtab recovered (4110); DS20 stripped; symbolicate DS20 via VT/BSim. Also: base-pinning blocked by a DS20 post-banner boot WALL
- **Working on:** Two threads converged — (a) the CPU1 base-pinning value/kick watch, and (b) the embedded-symtab recovery — and together they relocated the real blocker.
- **Done since last checkpoint:**
  - **Base-pinning is BLOCKED upstream, not a wiring gap.** Added value-watch + a TIG CPU-START **kick**-watch (`kIpcr0..3` PA `0x801_3000_0A00..0AC0`) in `MemDrainer::applyStoreEffect`. Cold DS20 to 268M: **no `START-WATCH(kick)` at all**, and `(val)` hits are only a vector/SCB relocation loop at `pc=0x600938` (writes `0x8001` across ~1.3MB, 93M–179M). The **108B** console log (`app_output_20260621150723.log`) shows ONLY the early `platform()` banner through 108e9 cycles — no device-probe output ⇒ `ddb_startup`/`start_secondaries` (after `ddb_startup(4)`, downstream of that banner) were **never reached**. So DS20 sits in a **post-banner boot-progress wall** for 100B+ cyc (run ended `PC=0x1adb60`/kFaultDtbMiss). `cpu_enabled` is NOT the cause (absent from `ds10_flash.rom` → `0xffffffff` default → bit1 set). Corrected the briefing's withdrawn "banner ⇒ passed start_secondaries" claim.
  - **107B banner state banked** as `predig_ds20_banner_cyc107000000000.axpsnap` (auto_'s moved aside); warm `./run_fw.sh ds20` resumes into it.
  - **Embedded symtab recovered for DS10.** `tools/symbolication/parse_symtab.py` → **4110 functions with linked addresses** (`ds10_v7_3_symbols.csv`), linked base `0x8000`, SYM array file `0x159f50`/ghidra `0x161f50`, name pool ghidra `0x1ad840..0x1c0068`; `ddb_startup=0x16f860`, `start_secondary=0x176bd0`, `powerup=0x173868`, etc. all resolve. **DS20 has NO symtab in ANY v7.3 variant** — decompressed + checked `DS20_V7_3.EXE/.SYS` and `PC264SRM.ROM/.SYS` (all symtab-free). DS20 is also a **different build** from DS10 (diverge at byte 4, ~14% identity), so DS10 addresses do not transfer.
- **Open / next:**
  1. Import DS10 at **base `0x8000`** + run `apply_symbols_ds10.py` → fully-named DS10 reference (analysis was running at checkpoint).
  2. **Symbolicate DS20 by porting DS10 names via Ghidra Version Tracking / BSim** (structural match, address-independent, high coverage — apisrm source shared); string-anchoring pipeline as fast partial cross-check.
  3. Then **retire-PC profiler over the post-banner wall** (~182M→current) with the names applied → identify the function that gates powerup from reaching `ddb_startup(4)` → `start_secondaries`. Base-pinning (CPU1) resumes only after the wall clears.
- **Watch-outs:**
  - **UNCOMMITTED temp diagnostic:** the `START-WATCH (val)`+`(kick)` block in `pipelineLib/MemDrainer.h` (env `EMULATR_START_WATCH`) is REMOVE-BEFORE-COMMIT — do not let it ride into a real commit. The LL/SC→LockMonitor rewire (snapshot v10) and the TIG CPU-START model + `Ev6Pc264PalDefs.h` + TIG doctest were built (472 green) and committed earlier this session; this watch is on top, uncommitted.
  - Don't re-run short value/kick watches or warm-resume-from-107B expecting a `(kick)` — `start_secondaries` is far beyond 107B, behind the wall.
  - Alpha rebuild gives DS20 *names* but not addresses matching the shipped (stripped) image — not usable for runtime PCs.
  - Memory updated: `reference_srm_symbolication`, `reference_secondary_cpu_start_tig` (base-pinning-blocked note).
