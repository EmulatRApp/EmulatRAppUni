<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="../helpproject.xsl" ?>
<topic template="Default" modified="2026-06-14T23:48:08.393Z" lasteditedby="tim" version="2" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../helpproject.xsd">
  <title>Execution / Boot Internals</title>
  <keywords>
    <keyword>BoxResult</keyword>
    <keyword>entry vector</keyword>
    <keyword>EXC_ADDR</keyword>
    <keyword>ExecCtx</keyword>
    <keyword>faultCode</keyword>
    <keyword>g_primaryTable</keyword>
    <keyword>grain</keyword>
    <keyword>HW_REI</keyword>
    <keyword>IFetchOverride</keyword>
    <keyword>MemDrainer</keyword>
    <keyword>PALcode trap delivery</keyword>
    <keyword>PipelineDriver</keyword>
    <keyword>PipelineSlot</keyword>
    <keyword>retire</keyword>
    <keyword>six-stage pipeline</keyword>
  </keywords>
  <body>
    <header>
      <para styleclass="Heading1">Execution / Boot Internals</para>
    </header>
    <para styleclass="Normal">This appendix is the implementation-level companion to the Execution Flight Plan. It walks one instruction through V4&apos;s six pipeline stages, then describes how a fault is delivered to PALcode and how the firmware boot path is fed during the decompression loops. The code is in <text styleclass="Code Example">pipelineLib/PipelineDriver.h</text>; the entry-vector table is in <text styleclass="Code Example">coreLib/Ev6EntryVectors.h</text>.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">The six-stage step</para>
    <para styleclass="Normal">EmulatR runs a functional, single-slot pipeline: each instruction is carried by a <text styleclass="Code Example">PipelineSlot</text> through all six stages in order before the next is fetched. The pipeline driver is the only component that mutates <text styleclass="Code Example">cpu.pc</text> -- the leaves and the memory drainer never touch it.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Normal"><table styleclass="Default" rowcount="7" colcount="2" style="border-width:0px; border-spacing:0px; border-collapse:collapse; cell-border-width:1px; border-color:#000000; border-style:solid; head-row-background-color:#00ffff; alt-row-background-color:none;">
      <thead style="vertical-align:top">
        <td>
          <para styleclass="Normal"><text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">Stage</text></para>
        </td>
        <td>
          <para styleclass="Normal"><text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">Work</text></para>
        </td>
      </thead>
      <tr style="vertical-align:top">
        <td>
          <para styleclass="Code Example">IF</para>
        </td>
        <td>
          <para styleclass="Normal">Mint a slot from cpu.pc; translate the VA to a PA (via translateInstruction) and read the 4-byte instruction word. The fetch consults an optional IFetchOverride first.</para>
        </td>
      </tr>
      <tr style="vertical-align:top">
        <td>
          <para styleclass="Code Example">DE</para>
        </td>
        <td>
          <para styleclass="Normal">Dispatch lookup against <text styleclass="Code Example">grainFactory::g_primaryTable</text> (plus the sparse secondary tables) to bind the decoded grain leaf.</para>
        </td>
      </tr>
      <tr style="vertical-align:top">
        <td>
          <para styleclass="Code Example">GR</para>
        </td>
        <td>
          <para styleclass="Normal">Build the ExecCtx: read the register file (integer or FP per the grain) into the execution context.</para>
        </td>
      </tr>
      <tr style="vertical-align:top">
        <td>
          <para styleclass="Code Example">EX</para>
        </td>
        <td>
          <para styleclass="Normal">Invoke <text styleclass="Code Example">grain.execFn(grain, ctx)</text> and store the returned BoxResult onto the slot (any memory effect is packed here, not performed).</para>
        </td>
      </tr>
      <tr style="vertical-align:top">
        <td>
          <para styleclass="Code Example">MEM</para>
        </td>
        <td>
          <para styleclass="Normal"><text styleclass="Code Example">MemDrainer::drain(slot, cpu, memory)</text> -- translate any data effect, perform the load/store, sign-extend loads, commit the register file, and resolve LL/SC.</para>
        </td>
      </tr>
      <tr style="vertical-align:top">
        <td>
          <para styleclass="Code Example">WB</para>
        </td>
        <td>
          <para styleclass="Normal">Advance cpu.pc from the grain&apos;s next PC, a divert target, or a fault entry vector; intercept HALT and faults; increment the cycle count.</para>
        </td>
      </tr>
    </table></para>
    <para styleclass="Normal"></para>
    <para styleclass="Normal">The driver exposes <text styleclass="Code Example">step</text> (one slot through all six stages) and <text styleclass="Code Example">run(maxCycles)</text>. The hot loop ships optimized; an <text styleclass="Code Example">EMULATR_DEBUG_STEP</text> CMake option restores an -O0 build for single-stepping.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">Fault delivery to PALcode</para>
    <para styleclass="Normal">Faults travel as a <text styleclass="Code Example">faultCode</text> on the slot&apos;s BoxResult and are resolved at WB by <text styleclass="Code Example">retire</text>. HALT is a graceful stop, not a trap: it sets <text styleclass="Code Example">cpu.halted</text> and returns. Any other non-zero fault is delivered to PALcode by the sequence in the EV6 HRM (6.8.1): save the faulting PC in <text styleclass="Code Example">EXC_ADDR</text> (bit 0 carries the pre-trap PALmode so HW_REI restores it on return), map the fault to an entry-vector offset with <text styleclass="Code Example">coreLib::ev6::entryForFault</text>, divert PC to <text styleclass="Code Example">palBase + offset</text>, and enter PAL mode. The mapping covers the architectural classes -- for example <text styleclass="Code Example">UNALIGN at 0x280, OPCDEC at 0x400</text>, <text styleclass="Code Example">DFAULT at 0x380</text>, <text styleclass="Code Example">DTB single-miss at 0x300, ITB miss at 0x580</text>, <text styleclass="Code Example">MCHK at</text>&#32;<text styleclass="Code Example">0x500</text> -- on the <text styleclass="Code Example">0x80</text>-spaced vector grid from <text styleclass="Code Example">0x100 to 0x780</text>. The absolute target is <text styleclass="Code Example">computeHwExceptionEntry(palBase, offset)</text>, which ORs the offset into the 32K-aligned palBase.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Normal">Delivery falls back to halt-with-diagnostic only when PALcode is not loaded (<text styleclass="Code Example">palBase == 0</text>, where diverting would land in low-memory garbage and lose the failure) or when the fault has no architectural entry (<text styleclass="Code Example">kEntry_None</text>). Routine high-volume TB misses (kFaultDtbMiss / kFaultItbMiss) are excluded from the fault log; everything else is recorded to <text styleclass="Code Example">logs/faults.log</text> for offline review.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">Feeding the boot path</para>
    <para styleclass="Normal">During the firmware&apos;s self-decompression loops the IF stage consults an optional <text styleclass="Code Example">IFetchOverride</text> before the normal translate-and-read, so fetches can be served from an immutable SRM payload while the guest rewrites memory underneath itself. The cold-boot inflate is the dominant early cost (see Decompressor Internals). Beyond decompression, the current runtime blocker is downstream: a clock-interrupt return through <text styleclass="Code Example">hw_ret R2</text> (the genuine instruction at PC 0xd954 confirmed by the host decompressor oracle), tracked on the Current Development Status page.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Normal"><text styleclass="SeeAlso">See Also.</text></para>
    <para styleclass="Normal"><link displaytype="text" defaultstyle="true" type="topiclink" href="--Execution-Flight-Plan-------">Execution Flight Plan </link></para>
    <para styleclass="Normal"><link displaytype="text" defaultstyle="true" type="topiclink" href="--Memory-Map--IPRs">Memory Map &amp; IPRs</link></para>
    <para styleclass="Normal"><link displaytype="text" defaultstyle="true" type="topiclink" href="--Current-Development-Status--">Current Development Status</link></para>
    <para styleclass="Normal"><link displaytype="text" defaultstyle="true" type="topiclink" href="Clock-Frequency--MHz">Clock Frequency and the MHz Reading</link></para>
  </body>
</topic>
