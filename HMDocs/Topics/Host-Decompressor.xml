<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="../helpproject.xsl" ?>
<topic template="Default" modified="2026-06-15T11:52:46.068Z" lasteditedby="tim" version="2" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../helpproject.xsd">
  <title>Host Decompressor</title>
  <keywords>
    <keyword>AXPBox</keyword>
    <keyword>build.sh</keyword>
    <keyword>decom.c</keyword>
    <keyword>decompressed_ds10_v7_3.bin</keyword>
    <keyword>es45_v7_3.exe</keyword>
    <keyword>host_decompressor</keyword>
    <keyword>inflate.c</keyword>
    <keyword>Mark Adler</keyword>
    <keyword>oracle</keyword>
    <keyword>oracle.c</keyword>
    <keyword>ref_decompressed.bin</keyword>
    <keyword>regression guard</keyword>
    <keyword>WimC</keyword>
  </keywords>
  <body>
    <header>
      <para styleclass="Heading1">tools/Host Decompressor</para>
    </header>
    <para styleclass="Normal"><text styleclass="Code Example">tools/host_decompressor</text> is a native, host-compiled build of the original DEC SRM Huffman / inflate decompressor. It serves as a trusted reference -- an oracle -- for the compressed Alpha firmware images, producing the decompressed firmware byte-for-byte without running any emulation. This page is the tool view (build, run, outputs); the algorithm and its place in the boot path are covered under Decompressor Internals.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">Why it exists</para>
    <para styleclass="Normal">The guest firmware self-decompresses on the emulated CPU at every cold boot (about 4 million cycles -- the spin at PC <text styleclass="Code Example">0x60111c</text>). To confirm that EmulatR&apos;s CPU produces the correct decompressed image without trusting either EmulatR&apos;s own CPU or AXPBox, the tool builds the same algorithm DEC used, from the same sources the firmware was compiled from, as a native host program. The reference image it produces matches EmulatR&apos;s emulated-CPU output byte for byte, which makes it both a regression guard and a clean way to mint a decompressed ROM. It is strictly stronger as an oracle than AXPBox&apos;s <text styleclass="Code Example">decompressed.rom</text>, which is generated by running the guest decompressor on AXPBox&apos;s own emulated CPU and so still depends on a correct CPU and a contrived entry sequence.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">Sources</para>
    <para styleclass="Normal">Under <text styleclass="Code Example">src/</text>, pinned copies of the read-only DEC originals plus the harness:</para>
    <list id="0" type="ul" listtype="bullet" formatstring="&#183;" format-charset="SYMBOL_CHARSET" levelreset="true" legalstyle="false" startfrom="1" styleclass="NotesBox" style="font-family:Symbol; font-size:11pt; color:#000000;">
      <li styleclass="NotesBox"> inflate.c -- Mark Adler inflate, version c10p1 (the engine).</li>
      <li styleclass="NotesBox"> decom.c -- the DEC wrapper (main + decompress); reference only, not compiled.</li>
      <li styleclass="NotesBox"> decomp.h -- types and the ROM / non-ROM switches.</li>
      <li styleclass="NotesBox"> ev6_huf_decom.m64 -- the guest assembly startup that calls decompress(); reference only (it also performs CPU / PAL state setup, which any in-emulator host path must preserve).</li>
      <li styleclass="NotesBox"> oracle.c -- the harness: it locates the WimC header, sets the input / output pointers and compressed size, calls inflate(), and dumps the image.</li>
    </list>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">Build and run</para>
    <para styleclass="Normal">Build with the bundled script (<text styleclass="Code Example">build.sh</text> on Linux / gcc, <text styleclass="Code Example">build.bat</text> on Windows), then run the oracle against a compressed firmware image:</para>
    <para styleclass="Normal"></para>
    <para styleclass="Code Example">sh build.sh</para>
    <para styleclass="Code Example">./oracle &lt;path-to-compressed-firmware.exe&gt;</para>
    <para styleclass="Code Example"></para>
    <para styleclass="Normal">Output is written to <text styleclass="Code Example">ref_decompressed.bin</text> with a printed summary; a known-good run of <text styleclass="Code Example">es45_v7_3.exe</text> is cached under <text styleclass="Code Example">out/</text>. The <text styleclass="Code Example">build_firmware_variants.sh</text> helper drives the oracle across multiple firmware images (the DS10 image, for example, yields <text styleclass="Code Example">decompressed_ds10_v7_3.bin</text>, which is the input the Ghidra workflow analyzes).</para>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">Compressed-image layout</para>
    <para styleclass="Normal">The oracle finds the firmware payload by a linear scan for the <text styleclass="Code Example">WimC</text> magic (<text styleclass="Code Example">0x436D6957</text>). For <text styleclass="Code Example">es45_v7_3.exe</text> the header sits at file offset <text styleclass="Code Example">0x2400</text>; its longwords give the compressed size (<text styleclass="Code Example">0x1a6164</text>), the decompress target (<text styleclass="Code Example">0x8000</text>), and the start of the compressed data (WimC + 20 = file <text styleclass="Code Example">0x2414</text>). The decompressed output is <text styleclass="Code Example">0x30cc00</text> bytes (3,197,952).</para>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">The regression guard</para>
    <para styleclass="Normal">In the decompressed image the V4-runtime quad at PA <text styleclass="Code Example">0xd950</text> is <text styleclass="Code Example">0x7be2a000_b4de0030</text> -- the <text styleclass="Code Example">hw_ret R2</text> at <text styleclass="Code Example">0xd954</text> uses R2 (byte <text styleclass="Code Example">0xe2</text>), never R6 (<text styleclass="Code Example">0xe6</text>). This is the signature that settled the 2026-05-30 single-bit-PAL-corruption question: that instruction is genuine firmware, not a decompression artifact. If a future change makes the guest CPU produce R6 in that neighborhood, it is a CPU regression, not a fix.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Normal">Integrating the oracle as a runtime alternate decompression path (<text styleclass="Code Example">--decompress=inline|host|cache</text>) is deferred until the SRM reaches the <text styleclass="Code Example">&gt;&gt;&gt;</text> prompt; the recommended intercept-hybrid design is described under Decompressor Internals.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Normal"><text styleclass="SeeAlso">See Also.</text></para>
    <para styleclass="Normal"><link displaytype="text" defaultstyle="true" type="topiclink" href="--Decompressor-Internals">Decompressor Internals</link></para>
    <para styleclass="Normal"><link displaytype="text" defaultstyle="true" type="topiclink" href="--Ghidra">Ghidra</link></para>
    <para styleclass="Normal"><link displaytype="text" defaultstyle="true" type="topiclink" href="--Firmware-Decode--Decompressi">Firmware Decode &amp; Decompression</link></para>
  </body>
</topic>
