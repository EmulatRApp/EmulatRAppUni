<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="../helpproject.xsl" ?>
<topic template="Default" modified="2026-06-15T11:52:15.209Z" lasteditedby="tim" version="2" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../helpproject.xsd">
  <title>Overview</title>
  <keywords>
    <keyword>21264</keyword>
    <keyword>21272</keyword>
    <keyword>Alpha AXP</keyword>
    <keyword>BoxResult</keyword>
    <keyword>CBox</keyword>
    <keyword>Cchip</keyword>
    <keyword>CpuState</keyword>
    <keyword>Cypress CY82C693</keyword>
    <keyword>Dchip</keyword>
    <keyword>EBox</keyword>
    <keyword>EV6</keyword>
    <keyword>Ev6EntryVectors</keyword>
    <keyword>Ev6Translator</keyword>
    <keyword>ExecCtx</keyword>
    <keyword>FBox</keyword>
    <keyword>fpBox</keyword>
    <keyword>grain</keyword>
    <keyword>GrainMasterV4</keyword>
    <keyword>GuestMemory</keyword>
    <keyword>HW_MFPR</keyword>
    <keyword>HW_MTPR</keyword>
    <keyword>IBox</keyword>
    <keyword>IPR</keyword>
    <keyword>kseg</keyword>
    <keyword>LockMonitor</keyword>
    <keyword>Machine</keyword>
    <keyword>MBox</keyword>
    <keyword>PalBox</keyword>
    <keyword>PALcode</keyword>
    <keyword>Pchip</keyword>
    <keyword>Pic8259Pair</keyword>
    <keyword>PipelineDriver</keyword>
    <keyword>PipelineSlot</keyword>
    <keyword>SPAM</keyword>
    <keyword>SPAMShardManager</keyword>
    <keyword>SRM</keyword>
    <keyword>TLB</keyword>
    <keyword>TlbEpoch</keyword>
    <keyword>TsunamiChipset</keyword>
    <keyword>Typhoon</keyword>
    <keyword>VirtualAlloc</keyword>
  </keywords>
  <body>
    <header>
      <para styleclass="Heading1">Overview</para>
    </header>
    <para styleclass="Normal">EmulatR is a full-system, high-fidelity emulator of the DEC Alpha AXP platform -- specifically the 21264 (EV6) processor and the 21272 (Tsunami / Typhoon) core logic as integrated in AlphaServer DS10 / ES40 / ES45-class systems. It is implemented in modern C++ (C++20) with a deliberately framework-free architectural core (Qt is confined to the periphery -- console UI and host glue), built with CMake. Its purpose is not to approximate an Alpha; it is to be one, faithfully enough that unmodified DEC SRM console firmware, PALcode, and the operating systems that ran on the metal run on EmulatR without knowing the difference.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Normal">This page describes the architecture of the current implementation (V4) -- the execution model, the memory and translation system, the privileged boundary, and the device fabric -- and the standard every subsystem is measured against. The chapters that follow expand each area; the deep-dive appendices carry the register-level detail.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">The fidelity standard and its authoritative sources</para>
    <para styleclass="Normal">EmulatR is governed by one standard: it must behave like real Alpha hardware in every architecturally visible way, even when that complicates the implementation. Correctness is defined by primary sources -- the <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">Alpha Architecture Reference Manual</text> and the original <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; font-style:italic; color:#000000;">DEC SRM console and PALcode sources</text>. These define what correct behavior is.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Normal">The test is black-box and adversarial, conducted by the most demanding guests available: the SRM console and PALcode exercise the privileged architecture directly, and a booting operating system exercises the memory model, the trap machinery, and the device fabric under real workloads. A second emulator (AXPBox) is consulted only as a convenience cross-check for differential triage -- it is explicitly <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">not</text> an authoritative source, because another implementation can be wrong, whereas the architecture manual and the firmware sources cannot. If a guest can distinguish EmulatR from an AlphaServer through architecturally defined behavior, the emulator is wrong.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">Primary goals and objectives</para>
    <para styleclass="Normal">1. <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">Architectural correctness first.</text> EmulatR models Alpha&apos;s architectural semantics, not a particular chip&apos;s timing. Correctness always outranks speed.</para>
    <para styleclass="Normal">2. <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">Deterministic, debuggable execution.</text> Execution is reproducible and inspectable. The run loop is staged and cycle-counted, and the hot path is deliberately shallow so a stack trace at any point stays short and legible.</para>
    <para styleclass="Normal">3. <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">Explicit, per-instruction semantics.</text> Every instruction resolves to one faithful grain leaf; behavior is never inferred from an operand-shape family. Architectural results and exceptions become visible only at the Writeback stage.</para>
    <para styleclass="Normal">4. <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">Separation of concerns.</text> Decode, integer, floating-point, memory, control, and privilege are isolated into well-defined functional domains -- the &quot;Boxes&quot; -- with one-way dependency flow. No domain reaches into another&apos;s internal state.</para>
    <para styleclass="Normal">5. <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">SMP-aware by construction.</text> Per-CPU state, CPU-local translation buffers, and load-locked / store-conditional reservation tracking are built in, so a multiprocessor build is an extension rather than a rewrite (see the scope note below).</para>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">The execution model</para>
    <para styleclass="Normal">Execution is driven by <text styleclass="Code Example">PipelineDriver</text> (pipelineLib) through six in-order stages: <text styleclass="Code Example">IF</text> (instruction fetch), <text styleclass="Code Example">DE</text> (decode / dispatch), <text styleclass="Code Example">GR</text> (grain-ready: read the register file into the execution context), <text styleclass="Code Example">EX</text> (execute), <text styleclass="Code Example">MEM</text> (memory drain), and <text styleclass="Code Example">WB</text> (writeback). Each instruction is carried through the six stages on a <text styleclass="Code Example">PipelineSlot</text>; architectural state -- the register file and the PC -- is committed only at <text styleclass="Code Example">WB</text>, the single well-defined commit point that makes exceptions precise and replay deterministic.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Normal">Dispatch is flat. At <text styleclass="Code Example">DE</text> a decoded instruction is resolved through the grain system to a single grain leaf -- a pure function with the signature <text styleclass="Code Example">BoxResult(InstructionGrain const&amp;, ExecCtx const&amp;)</text> -- that implements its semantics. Execution is partitioned into the functional Boxes, each a namespace of grain leaves rather than a class hierarchy: <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">IBox</text> (control flow -- branches, jumps), <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">EBox</text> (integer ALU), <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">FBox</text> (floating point, IEEE and VAX, over the fpBox backend), <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">MBox</text> (loads, stores, load-locked / store-conditional), <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">CBox</text> (branch prediction and target resolution), and <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">PalBox</text> (the privileged boundary). Dependencies flow one way: every Box depends on coreLib; most also on iprLib; MBox additionally on memoryLib.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Normal">A deliberate consequence is that the hot path is shallow -- from the driver, through the stage, into a grain leaf, the call-stack depth is typically two and rarely exceeds four. That keeps traces legible (serving debuggability) and minimizes the per-instruction scaffolding that profiling has shown -- rather than the opcode bodies -- to dominate runtime (serving performance without compromising correctness). The grain leaves are the leaves of that call tree by construction.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Normal">Dispatch tables are generated, not hand-maintained. <text styleclass="Code Example">GrainMasterV4.tsv</text> is the single source of truth for instruction semantics; the <text styleclass="Code Example">genGrains.py</text> codegen reads it and emits the dense primary table (indexed by the 6-bit opcode) plus the sparse secondary tables for multi-function opcodes, including the personality-aware PALcode dispatch (OSF/1 and VMS). There is no runtime trace cache or JIT; the generated tables are the decode artifact.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">Memory and address translation</para>
    <para styleclass="Normal"><text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">Physical backing.</text>&#32;<text styleclass="Code Example">GuestMemory</text> (memoryLib) is the single physical-memory interface -- a passive byte-store that pages the guest&apos;s physical address space in on demand, in 64 KiB pages, committed only as the guest first touches them, so a multi-gigabyte machine costs only its working set. The pages are backed by host <text styleclass="Code Example">VirtualAlloc</text> on Windows and by <text styleclass="Code Example">mmap</text> on Linux / ARM hosts (with a portable fallback). The earlier SafeMemory layer has been removed. <text styleclass="Code Example">GuestMemory</text> itself does no MMIO routing or fault generation: the <text styleclass="Code Example">TsunamiChipset</text> system-bus arbiter decodes addresses, routes device traffic, and raises range faults before raw RAM is ever touched. A <text styleclass="Code Example">LockMonitor</text> tracks per-CPU load-locked / store-conditional reservations at cache-line granularity.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Normal"><text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">Guest translation.</text> Address translation is performed by <text styleclass="Code Example">Ev6Translator</text> (mmuLib) as the EV6 architecture specifies: PAL-mode bypass, the canonical virtual-address window check, and superpage (kseg) mapping, with non-mapped accesses returning an ITB or DTB miss. The translation buffers are modeled by the <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">SPAM</text> cache -- Set-Partitioned Associative Memory -- as <text styleclass="Code Example">SPAMShardManager</text> instances resident in <text styleclass="Code Example">CpuState</text> (<text styleclass="Code Example">itbMgr</text> and <text styleclass="Code Example">dtbMgr</text>, each 16 shards by 8 ways = 128 entries). Invalidation is epoch-based and lazy (<text styleclass="Code Example">TlbEpoch</text>): a TBIA bumps a global counter and a TBIAP a per-process one, so a flush is O(1) and stale entries simply fail the liveness test at lookup. The translation buffers are CPU-local by design.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">Faults, exceptions, and the privileged boundary</para>
    <para styleclass="Normal">There is no central fault dispatcher. A leaf, or the memory drainer that detects a fault, sets a <text styleclass="Code Example">faultCode</text> on the instruction&apos;s <text styleclass="Code Example">BoxResult</text> (for example a DTB miss or an unaligned access). The hardware exception entry points are compile-time constants in <text styleclass="Code Example">Ev6EntryVectors.h</text> (the vectors at <text styleclass="Code Example">0x100</text> through <text styleclass="Code Example">0x780</text>, spaced <text styleclass="Code Example">0x80</text> apart). At <text styleclass="Code Example">WB</text> the driver inspects the fault: a halt sets the CPU&apos;s halted flag; otherwise, once <text styleclass="Code Example">PAL_BASE</text> is established, control diverts to the computed entry vector. Privileged operations -- <text styleclass="Code Example">CALL_PAL</text>, <text styleclass="Code Example">HW_MTPR</text> / <text styleclass="Code Example">HW_MFPR</text>, <text styleclass="Code Example">HW_REI</text> -- are handled in <text styleclass="Code Example">PalBox</text>, which applies IPR side-effects (PAL_BASE normalization, TLB invalidation, ASN context) inline. Internal processor registers are flat members of <text styleclass="Code Example">CpuState</text>.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">The machine and its device fabric</para>
    <para styleclass="Normal"><text styleclass="Code Example">Machine</text> (systemLib) is the top-level orchestrator: it owns the <text styleclass="Code Example">CpuState</text>, the <text styleclass="Code Example">GuestMemory</text>, and the <text styleclass="Code Example">TsunamiChipset</text>, and provides firmware load, reset, and the step / run entry points. The chipset aggregates the three 21272 components -- the <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">Cchip</text> (system control, memory arbitration, interval timers, DRAM array configuration), the <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">Dchip</text> (data path), and the <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">Pchip</text> (PCI host bridge) -- together with the on-board devices. The PCI fabric is a standard bus / slot / function hierarchy (there is no separate &quot;stratum&quot; abstraction).</para>
    <para styleclass="Normal"></para>
    <para styleclass="Normal">The southbridge is the <text styleclass="" style="font-family:Arial; font-size:10pt; font-weight:bold; color:#000000;">Cypress CY82C693</text> PCI-to-ISA bridge (<text styleclass="Code Example">Cypress_CY82C693ISABridge</text>), which carries the ISA / SuperIO plane. The modeled devices include the CY82C693 IDE / ATAPI channel, the SuperIO controller (FDC / COM / LPT), the TOY / RTC, a 16550 UART for the serial console, the PCF8584 IIC controller, the flash / SROM, and the dual 8259A interrupt controllers (<text styleclass="Code Example">Pic8259Pair</text>). SCSI endpoints (<text styleclass="Code Example">VirtualScsiDevice</text> and the ISO-CD variant) exist in the device library.</para>
    <para styleclass="Normal"></para>
    <para styleclass="Heading2">Lineage: four implementations, one set of objectives</para>
    <para styleclass="Normal">The current emulator is the fourth implementation; the objectives above were fixed from the start, and each version reached for them by a different mechanism.</para>
    <list id="0" type="ul" listtype="bullet" formatstring="&#183;" format-charset="SYMBOL_CHARSET" levelreset="true" legalstyle="false" startfrom="1" styleclass="NotesBox" style="font-family:Symbol; font-size:11pt; color:#000000;">
      <li styleclass="NotesBox">V1 dispatched each instruction through a virtual call to a per-instruction grain. It was correct and booted SRM, but the per-call overhead capped throughput.</li>
      <li styleclass="NotesBox">V2 chased performance by dispatching on instruction families (operand-shape signatures). It was fast, but families under-determined behavior -- instructions sharing a signature still needed per-instruction corner-case handling the model could not express.</li>
      <li styleclass="NotesBox">V3 tried to hybridize V1 and V2; the coupling was too tight and introduced silent-failure modes -- the worst outcome for an emulator whose value is fidelity.</li>
      <li styleclass="NotesBox">V4 keeps a flat dispatch path but resolves it to faithful per-instruction grain leaves via generated tables, with performance pursued as the sum of many marginal gains rather than one architectural bet. The dispatch mechanism evolved across all four; the objectives did not.</li>
    </list>
    <para styleclass="Normal"></para>
    <para styleclass="Normal"><text styleclass="" style="font-family:Arial; font-size:10pt; font-style:italic; color:#000000;">Scope note: this page describes the V4 architecture and the standard it is held to. Some elements are infrastructure ahead of full activation -- multiprocessor execution and cross-CPU IPI, full PAL trap delivery, and SCSI / PCI device enumeration. For what is implemented and working in the current build versus what is still in progress, see Current Development Status.</text></para>
    <para styleclass="Normal"></para>
    <para styleclass="Normal"><text styleclass="SeeAlso">See Also.</text></para>
    <para styleclass="Normal"><link displaytype="text" defaultstyle="true" type="topiclink" href="--Memory----------------------">Memory</link></para>
    <para styleclass="Normal"><link displaytype="text" defaultstyle="true" type="topiclink" href="--Addressing------------------">Addressing</link></para>
    <para styleclass="Normal"><link displaytype="text" defaultstyle="true" type="topiclink" href="--Execution-Flight-Plan-------">Execution Flight Plan</link></para>
    <para styleclass="Normal"><link displaytype="text" defaultstyle="true" type="topiclink" href="--Current-Development-Status--">Current Development Status</link></para>
  </body>
</topic>
